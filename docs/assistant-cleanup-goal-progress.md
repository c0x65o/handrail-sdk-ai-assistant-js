# Assistant cleanup goal progress

Goal: `06b47a97-7cd6-41b7-9040-6b88761094ce`.
Scope remains JS/React/server and Flutter SDKs plus Mills, Spartan/Aegis and
Hitcents/Cents web/mobile consumers. Overall completion is unproven.

Latest milestone: turn 36 completes the managed ordinary-upload expiry source
follow-up, fixes shared-file consumption and removes the retired staging alias.
Eighty focused cases, full TypeScript, scoped lint, normal build and
twelve native PostgreSQL cases pass. The source and the terminal-approval fix
remain newer than installed public 15a; neither is released by this goal.
The updated shutdown template also passes a fresh normal public-15a install,
clean reinstall, typed/build gates, static adoption and synthetic native runtime
qualification. See the turn 36 entry and the exact retention contract below.
The [current acceptance inventory](assistant-cleanup-acceptance.md) maps all six
criteria to source, installed evidence and outstanding release/runtime/data gates.

Turn 35 qualified the normal public adoption. All three
web consumers install JS 0.2.37 at `15a3806c2595a3f93a87a768ad13293113f41b58`
with matching locks, full typed compiles, builds and static adoption checks.
Mills passes 187 selected cases; Cents passes 152; Aegis passes 113 of 114.
Aegis's unchanged mounted terminal-approval assertion exposes a public SDK
response regression. The newer source fix passes 22 SDK cases and full
typecheck/lint/build, but remains unpublished and unadopted by installed hosts.

All three main mobile consumers normally install public Flutter
`50fe566d73f68b2beacc2a874dc9a038363b1509` (client 0.1.8/widgets 0.1.1) with matching
locks and public Git-cache roots. Mills' shared workspace is now in main and
passes full analysis, 312 selected cases and a release web build. Aegis passes
analysis, 179 selected cases and its web build; Cents passes analysis, 96 selected
cases and its web build. Cents's installed visual gate now passes at
0.12282185395988604 against unchanged 0.123, with six actual installed captures.
The test-font Greek glyph limitation remains. The detached Mills candidate also
uses normal public resolution, and redundant host WebRTC dependencies are removed.

The Flutter gateway fixture now normally installs public JS 15a; full client
analysis and all 138 cases pass, including its formerly skipped attachment case.
A new public-15a scaffold passes install, clean reinstall, both typed/build gates,
static adoption and a native disposable-PostgreSQL synthetic runtime check.

Turn 35 qualified atomic ordinary-upload admission, input snapshots and
lost-commit replay. Turn 36 adds an automatically owned bounded expiry lifecycle
for newly marked records, with no startup sweep or old-row adoption. Unresolved
text/voice/provider/tool work, shared files and required receipts remain protected.
Old unmarked records must remain for approved cutover. The turn 35 next-audit note
is historical; its worker and alias tasks are now implemented in local source.

Mills and Cents development Mobile Preview attempts returned
`preview_browser_access_denied` outside this Dev Chat's saved scope, matching the
earlier Spartan route rejection. No browser opened and no app failure was
reproduced. This does not establish every possible live route is unavailable.
Corrected SDK adoption, authorized runtime/native qualification and approved
production cleanup remain open. No releases or production writes occurred;
no overall completion or global blocked-audit streak is claimed.

## September 13 first autonomous continuation

Verified the native goal and durable Handrail goal are active. Current checkouts
show the earlier Mills legacy source removal is already present. Other work is
modifying shared React presentation and Mills UI concurrently; those edits were
preserved. This continuation focused on shared durable deletion.

Implemented the SQL history deletion fence, default catalog content cleanup,
SDK file cleanup, conversation-bound OpenAI continuation retention and focused
regressions. See [the exact deletion scope](conversation-deletion.md), including
what remains retained and why this is not a complete erasure claim.

Validation for this continuation: full SDK TypeScript check, scoped ESLint and
normal SDK build passed. The expanded run passed 73 tests across 13 files;
the final run passed 44 overlapping tests across four files, including all 17
new deletion SQL cases. Commands used Vitest with `--maxWorkers=1 --minWorkers=1`.
Logs: `/tmp/sdk-deletion-typecheck.log`, `/tmp/sdk-deletion-lint.log`,
`/tmp/sdk-deletion-expanded-tests.log`, `/tmp/sdk-deletion-final-tests.log` and
`/tmp/sdk-deletion-build.log`. This is SDK source qualification, not proof that
any deployed consumer has installed the changes.

## September 13 second autonomous continuation

Verified the native and durable Handrail goals remain active and kept the
objective's explicit release restriction. Project-scoped Coverage Q&A search for
human-answered assistant decisions returned no entries; no additional owner
requirement was inferred from that result.

Completed the next disposable-content retention step. OpenAI chat, title and
dictation provider operations now carry explicit conversation bindings. Deletion
blocks unfinished/uncertain provider work and converts completed responses to
minimal receipts. A restarted or stale request cannot parse deleted response
content or redispatch the provider. Dictation racing deletion gets a safe
unavailable-conversation response; audio usage evidence remains intact.

Catalog create/rename/clear/archive/restore receipts also contained duplicate
titles and metadata. Deletion now removes that content while retaining the keys
and fingerprints. Exact retries report `not_found`, changed requests conflict,
and permanent deletion remains idempotent. Business idempotency domains remain
unchanged. Transaction rollback restores the original catalog/provider results.

Also corrected the default catalog's false clear capability. Without a content
reset callback, clear is now explicitly unsupported and cannot report success
after merely updating a timestamp. The negotiated client/UI respects this
capability while retaining the other catalog actions. A shared safe content
reset remains outstanding; this capability correction does not implement it.

Final validation passed 82 tests in eight files, including 23 deletion SQL cases,
native title/transcription retention regressions and negotiated clear controls. Full SDK TypeScript,
scoped ESLint and normal build passed. Logs:
`/tmp/sdk-provider-retention-tests.log`,
`/tmp/sdk-provider-retention-typecheck.log`,
`/tmp/sdk-provider-retention-lint.log`,
`/tmp/sdk-provider-retention-build.log`. Vitest used one worker. PGlite is local
SQL qualification, not multi-instance PostgreSQL or real provider/audio evidence.

Corrected shared adoption guidance to include Mills' disposable-history decision
and the Flutter guide's stale instruction to preserve Mills reconciliation.
The Flutter adoption investigation also identifies a separate real-install
dependency constraint: Mills/Cents need a published compatible bug-reporter
revision before normal resolution with the tested Flutter assistant baseline.
That investigation and its pinned revisions require re-verification before any
adoption action. No dependency manifests/locks or release state changed here.

## Remaining acceptance work

1. Complete a current-source/doc remnant audit across registered consumers using
   the later milestones below. Older bullet lists are not a fresh inventory.
   Immutable applied migration history is retained deliberately. All three hosts
   have locally qualified empty-database baselines; production old-table removal
   remains an approved cutover operation.
2. Inventory and remove old imported SDK copies, unbound provider/continuation/
   transcription records and chat-only files without losing ownership, uncertain
   business outcome, audit or billing evidence. New file deletion/retention source
   is qualified; older unmarked staging requires explicit review. No startup or
   migration purge is authorized.
3. Adopt the newer JS/Flutter source through reviewed public full SHAs and locks,
   including Aegis's retained-file worker and Mills' shared mobile candidate.
   Cents' public Flutter visual measurement remains below its gate; the source
   correction passes locally and still requires publication/adoption. Cents'
   Clear-to-New duplicate and scoped mobile generic wrapper copies are removed.
4. Finish the actual runtime/provider/audio qualification matrix using authorized
   browser/Mobile Preview routes and QA Vault identities. Record each platform
   rejection precisely. Prior local conformance, clean install, build, native
   fixture and image checks are evidence only for the scope they exercised.
5. Perform the authorized coordinated host releases and native production cleanup
   before claiming rollout or overall completion. The exact objective currently
   prohibits commits, pushes, PRs, SDK publication and deployment. Production SQL
   also needs the separate interactive approval mechanism.

The shared source, consumer installation, runtime behavior and production data
removal are separate completion facts. Turn 34 makes verified source progress;
it is not a blocked-audit turn.

## September 13 third autonomous continuation

Reverified both goals and the current source. The preceding retention turn made
code/test progress. This continuation addressed Mills' fresh database schema and
leftover attachment linkage; it also makes progress and is not a blocked audit.

Mills' normal migration command now installs a verified baseline only on empty
databases. The baseline preserves the business/native SDK schema from 99 original
migrations while omitting four retired chat tables, two enums, the old attachment
message column and message-specific triggers/functions. Installation atomically
records the original prefix and a separate baseline-origin receipt. Existing
schemas and prior migration rows remain untouched. New migrations continue
through ordinary Drizzle behavior. A non-mutating 0099 snapshot boundary prevents
future generation from reintroducing automatic destructive legacy cleanup.

Removed Mills' unused attachment message field/read and its obsolete modeled
constraint/index predicate. New attachment readiness uses the retained media and
canonical SDK authorization paths. The fresh schema retains file protection and
enforces immutable household/conversation/media ownership. Existing production
linkage/trigger removal is still an explicitly approved cutover step.

Qualification: 31 gateway tests passed against the actual fresh baseline; five
baseline tests passed for business schema parity, unchanged historical install,
future migration/replay, tamper rejection/rollback, non-empty refusal and current
snapshot alignment. Native PostgreSQL 15 CLI checks also passed on disposable
fresh/historical databases: fresh installation/repeat retained exactly 100 ledger
entries, while the historical public schema dump and original 99 ledger entries
were unchanged, with only the non-mutating boundary added. All scratch PostgreSQL
processes created for this work were stopped. No project database was used.
Full Mills TypeScript, scoped ESLint, normal server compilation, Drizzle ledger
checks and whitespace checks passed. Runtime-source searches found no remaining
references to the four retired tables or the old attachment message linkage.

See Mills `docs/fresh-database-baseline.md` for the contract, maintenance command,
cutover limits and log locations. This does not install the unpublished SDK
deletion candidate into Mills or complete consumer/runtime/production cleanup.

## September 13 fourth autonomous continuation

Reverified the active native/Handrail objectives and bounded working-project
access. Preserved unrelated presentation edits and the goal's explicit release
restriction. This continuation makes additional SDK source/test progress; it is
not a blocked-audit turn.

Added a trusted PostgreSQL catalog table mapping so retained business ownership
tables do not require another generic catalog implementation. It supports
canonical UUID ownership columns, keyset pages, archive/restore, the shared
permanent-delete transaction and soft-deletion filtering. Added deterministic
host title policy and a transactional audit hook. Failed audit/precommit auth
checks roll back ownership, history, deletion fence and durable receipts.

New catalog creation claims a minimal tenant/conversation ownership identity
across mapped/native catalogs. Conflicting table/owner claims cannot acquire or
read the same identity; SQL filters those claims before pagination limits.
Mapped storage identity participates in idempotency fingerprints. Retained
ownership claims contain no title/transcript. Older unregistered catalog copies
still require the coordinated disposable-chat cutover.

Native postgres.js qualification exposed double encoding of already serialized
JSON parameters, which Mills currently works around in host code. SDK-owned SQL
now casts those parameters through text before jsonb across persistence, catalog
and incident stores. A plain transactional postgres.js adapter now reads back
nested state and effect receipts and replays without redispatch or host rewriting.
Existing malformed rows are not rewritten by this source change.

Validation: full SDK TypeScript, scoped ESLint, normal build and 113 tests across
20 files passed with one Vitest worker. Eight new mapped-catalog SQL tests cover
lifecycle, pagination, ownership/FKs, audit rollback, history deletion, global
claims and canonical IDs. A standalone native PostgreSQL 15 fixture passed JSON
readback/effect replay plus five independent-connection concurrency cases:
competing owners, competing storage tables, deletion versus late append,
admission versus deletion, and competing expected-version renames. The fixture
observes actual database lock waits, accepts no project connection, and closes
its disposable cluster. An initial diagnostic attempt left a fixture after an
unhandled rejection; that exact owned cluster was stopped and removed, and the
fixture's failure cleanup was corrected before the final passing run. No project
SQL/storage writes or dependency/release actions occurred.

Logs: `/tmp/sdk-catalog-mapping-typecheck.log`,
`/tmp/sdk-catalog-mapping-lint.log`, `/tmp/sdk-catalog-mapping-build.log`,
`/tmp/sdk-catalog-mapping-final-tests.log` and
`/tmp/sdk-catalog-native-concurrency.log`. Reproduction and storage contracts are
in [postgres-catalog-mapping.md](postgres-catalog-mapping.md).

Next source work: Mills still uses its bespoke catalog adapter and installed old
SDK pin. Prepare its additive lifecycle/metadata columns, shared catalog gateway
wiring, minimal domain audit hooks and durable external-media cleanup. The new
SDK APIs require an eventually authorized public committed SHA and matching
consumer lock. No temporary alias or local source fixture establishes adoption.
Continue the remaining JS/Flutter consumer, generic feature, clean-install and
retention work from the full acceptance list. Native local SQL concurrency is
additional evidence, not deployed provider/audio/browser qualification.

## September 13 fifth autonomous continuation

Reverified the native goal, durable goal and bounded access at turn start.
The previous turn completed mapped-catalog and JSON/concurrency work and is
classified as progress. This turn also changes source and qualification evidence;
there is no blocked-audit streak.

Prepared Mills' retained-table adoption module in
`src/server/assistant/handrail-sdk-catalog.ts`. It delegates generic catalog
operations to the SDK and retains live-session/household/user checks, title
redaction, actor attribution and read/mutation audit. A required host file-cleanup
callback prevents silently omitting that integration. Additive migration 0100
provides lifecycle, archive timestamp, metadata, constraints and an owner index.
It preserves earlier migrations, business foreign keys and the fresh baseline;
it performs no legacy purge. A corresponding model snapshot/down script is
prepared, with no database deployment.

Added the SDK `onRead` audit hook, which runs only for a successful get/list,
checks access before returning data, and prevents a successful response if the
audit fails. This keeps Mills' read audit without another catalog implementation.
Full SDK typecheck, scoped lint, normal build and 13 focused tests passed.
Logs: `/tmp/sdk-catalog-read-audit-{typecheck,lint,build,tests}.log`.

Mills candidate qualification passed full TypeScript checking, server compilation
and nine tests (four catalog plus five baseline/schema cases). Scoped lint and
Drizzle checks passed. The helper uses explicit temporary source aliases, emits
server output only into its removable fixture directory, and verifies unchanged
package/lock hashes. It neither installs a local SDK dependency nor establishes
actual adoption. Logs: `/tmp/mills-sdk-catalog-candidate-check.log`,
`/tmp/mills-sdk-catalog-lint.log`, `/tmp/mills-sdk-catalog-drizzle-check.log`.

The ordinary installed-pin typecheck was also run and fails at the new `table`
option with consequent missing hook types. The current Mills pin does not contain
these APIs. This explicit unfinished build/adoption dependency is recorded at
`/tmp/mills-sdk-catalog-installed-typecheck.log`. No fallback, copied SDK runtime,
manifest substitution, commit or release hides it.

The new Mills module is still not connected to the production gateway; its file
callback test double proves composition only. Next, implement durable cleanup of
host attachment links and chat-only media, preserving every business/shared
reference and guarding in-flight/signed uploads. Then wire the factory through
the gateway, remove old generic catalog store methods/pagination, and qualify
actual gateway behavior against the candidate. Existing protected uploads use
Mills `ObjectStorage.deleteUploadObject`; saves to business documents copy to new
objects. Current metadata deletion leaves `media_objects`/remote bytes behind.
Any old signed upload intents or unresolved writes must be accounted for before
declaring remote deletion final. The broader JS/Flutter, consumer-install,
runtime and production cutover requirements remain intact.

## September 13 sixth autonomous continuation

Reverified native/durable goals and bounded access at turn start. Both remained
active with the exact full objective. The prior turn made source progress and
this turn also completed substantial source/qualification work; there is no
blocked-audit streak. No commit, push, PR, publication, deployment, project SQL
write or remote object mutation occurred. Concurrent UI changes were preserved.

Implemented the SDK's postcommit external-file queue and worker in
`src/postgres/conversation-file-cleanup.ts`. The enqueue operation requires the
sealed conversation inside the authorized SQL transaction. Jobs bind a trusted
service partition, tenant, immutable object and fingerprint. Draining validates
host ownership/deployment policy, serializes workers with row locks, retries
uncertain idempotent deletes with timeout/abort and backoff, and removes target
content from completed receipts. Invalid targets remain blocked for diagnosis.
The worker joins flushes and drains its bounded active batch on stop. It never
inventories/purges old chats at startup.

Mills now wires the shared catalog factory into its actual assistant gateway.
Removed the generic `handrail-conversation-catalog.ts` adapter and host list,
create, rename and catalog-only delete store methods. Retained domain ownership
reads, file/provider authorization and financial review integrations. The SDK
catalog now supports optional event-write activity and millisecond keyset
precision, preserving Mills' trusted recent-activity ordering without host
pagination. Client-supplied event timestamps cannot reorder the catalog.

Mills' deletion callback removes chat attachment links transactionally, retains
every shared/business/unrecognized media object, deletes only exclusively owned
canonical media rows, and queues their remote bytes. It scrubs disposable upload
and old catalog response copies but preserves request identities and business
receipts. Current assistant uploads no longer generate unused signed storage
URLs; authenticated PUTs durably record conversation-bound dispatch before
writing storage and hold ownership locks. Unknown write outcomes prevent both
redispatch and conversation deletion. Worker startup/shutdown is wired into the
application; safe diagnostics expose counts, not keys or provider details.

Qualification:
- Full SDK typecheck/build, scoped lint and 43 focused tests passed. Four further
  SQL pagination/activity cases brought the mapped-catalog suite to 13 cases;
  these and the four default catalog cases passed with a fresh typecheck/build.
- The owned PostgreSQL 15 fixture passed the native postgres.js JSON contract and
  seven independent-connection concurrency cases, including postcommit queue
  visibility, competing file workers, and upload dispatch versus deletion. It
  stopped/removed its disposable cluster; no project connection was accepted.
- Mills source-candidate typecheck/server compile and 88 tests passed: 34
  assistant, 34 real gateway, seven file/catalog, three gateway auth, five domain
  ownership and five baseline cases. Generic catalog client/archive/restore/
  reopen and mid-read reauthorization checks moved from permissive unit doubles
  into the real migrated gateway fixture. A final seven-case run exercised file
  deletion through the actual HTTP gateway; full candidate typecheck/compile and
  scoped lint passed again. Shared/business bytes remained in the fake store.
- The first broad candidate run exceeded 6 GB because it bypassed Mills' reusable
  database template. Its verified owned processes were stopped. The candidate
  runner now uses the established shared-fixture harness, and isolates file-owned
  and fresh-database tests in separate processes. Final checks use one worker.
- The ordinary installed-pin typecheck was rerun and **fails**: the old public
  SDK lacks the mapped catalog, cleanup exports and bound/deleted provider APIs.
  This is an explicit release/adoption build dependency. Manifests/locks stayed
  unchanged; temporary validation aliases are not installed-consumer evidence.

Logs: `/tmp/sdk-file-cleanup-typecheck-final.log`,
`/tmp/sdk-file-cleanup-expanded-tests.log`, `/tmp/sdk-catalog-activity-tests.log`,
`/tmp/sdk-catalog-activity-{typecheck,build}.log`,
`/tmp/sdk-file-cleanup-activity-{lint,native}-final.log`,
`/tmp/mills-sdk-catalog-wiring-check-v2.log`,
`/tmp/mills-file-cleanup-gateway-final.log`,
`/tmp/mills-file-cleanup-wiring-lint-final.log`,
`/tmp/mills-sdk-catalog-installed-typecheck-final.log`.

Next independent source work remains the full JS/Flutter consumer and generic
feature audit, safe reusable-conversation Clear/reset, and remaining chat-derived
retention. The seventh-turn section below supersedes the scaffold/setup status.
Revisit older unbound
records, unknown media and signed/old-worker upload exposure during coordinated
cutover; current queue completion is not proof those old bytes are gone. Public
committed SDK adoption, the declared Flutter dependency constraints, actual
provider/audio/browser checks, and approved production cleanup remain separate
requirements. The objective explicitly withholds release authorization, and
production SQL requires native interactive approval. Do not mark the goal
complete or claim 100% while those requirements remain.

## Seventh autonomous turn: clean setup and a new-history concurrency defect

The same full Handrail/native goal was reverified active; bounded goal access
remains working-project scoped. SDK Coverage Q&A search returned no human
answers. No release, SDK publication, project database or storage writes occurred.
Unrelated changes remain preserved. The original complete JS/Flutter and
Mills/Spartan/Hitcents scope has not been reduced.

### Setup source and actual public Git installation

- Replaced the three-file composition-only template with a complete typed
  React/Vite/Express host: manifest, build configuration, supported SDK imports,
  standard launcher, explicit SDK migration, environment doctor, safe structured
  diagnostics, host identity/CSRF seam, empty denied domain tools and pool/usage
  shutdown. Default auth returns `host_auth_not_configured`; no development login
  or trusted-header shortcut is shipped. Migration is explicit, never a startup
  purge. The host must still supply authenticated sessions and business policy.
- Scaffold resolves public Git HEAD once and writes a full SHA, or honors an
  explicit frozen 40-character revision. It refuses nonempty targets and does
  not install dependencies or mutate databases. npm/gitignore templates are
  packaged under ordinary filenames and copied as dotfiles so npm's package
  filters cannot silently drop their configuration.
- Real installation caught the old template's incorrect `openaiResponses`
  import and npm 10's HTTPS-to-SSH lock rewrite. The scaffold now requires a
  compatible Node and npm 12.0.2+ (pacote's HTTPS fix). It explicitly allows
  root Git dependencies and grants SDK compilation to the exact Git source/SHA
  in `allowScripts`; matching only the package name does not authorize a Git
  dependency's build in npm 12. esbuild has its own script permission. No global
  npm upgrade, SDK tarball installation, source alias or packaging step was used.
- Final unmodified generated fixture
  `/tmp/handrail-clean-scaffold-217786b0-7-final-source` resolved public commit
  `5d9387c1a07b4b131cbc65ce273a5d1f6faf035b`, version `0.2.35`. Both a fresh
  `npm install` and `npm ci` compiled the SDK normally; typed client/server and
  production build passed. Manifest, lock root and installed `resolved` match
  the exact public HTTPS SHA. Source adoption check passed. These receipts
  establish the scaffold source and installation/build, not unpublished feature
  adoption. The published CLI/template itself is still the old revision.
- Static adoption checks now reject a different/missing/non-Git resolved SHA,
  linked SDK nodes, conflicting dependency groups, nested legacy nodes and
  duplicate canonical SDK copies. Results explicitly say
  `static-source-and-lockfile-only` rather than claiming runtime parity.

### Runtime investigation and source correction

`scripts/check-scaffold-install.mjs` imports only the generated composition and
its installed public SDK. It accepts no database URL, creates its own native
PostgreSQL cluster, runs the generated migration twice, and exercises real
Express authentication, 401/403, user+tenant isolation, live permission changes,
new send/history, new server+pool+client reload, archive/restore and usage delivery
after a synthetic outage. Provider/auth/usage fixtures never enter product
source. Every invocation closes its clients/listeners/pools and removes the
owned cluster. It performs no real provider or audio work.

Some public-pin runs passed those checks, but others failed first send with an
internal error. **The public baseline is not considered fully runtime-qualified.**
Investigation found `PostgresConversationEventStore.read()` returning its event
page and head from separate statements in a READ COMMITTED transaction. A new
append between those statements can make replay report a false revision gap.
The native concurrency fixture now commits exactly in that interval and failed
before the fix (`latestRevision` 2 versus the page's 1). The source uses one SQL
statement/snapshot for page and head; the regression passes, and the subsequent
read sees the concurrent event. Empty/tail pages and numeric pagination are
covered too. This changes no production schema or data.

The public-pin fixture intentionally retains its checks; do not disable recovery,
add timing workarounds or alias a local SDK to call adoption complete. A committed
SDK containing the correction and a fresh installed runtime run remain a release
dependency. See [scaffold qualification](./scaffold-qualification.md).

### Validation receipts

- SDK full typecheck/build passed. The targeted SQL/history/title/deletion and
  public-adapter run passed 42/43 initially; the remaining assertion was an
  unrelated stale example/public-limit distinction and was corrected without
  changing runtime limits. Both Spartan adapter suites then passed (4 tests).
  Public adapter/package expectations now match the committed 150-call limit;
  the standalone historical example retains its separate 75-call setting.
- Native JSON contract plus eight PostgreSQL concurrency cases passed, including
  page/head, ownership, deletion fencing, admission, rename, external file jobs
  and upload exclusion. All disposable clusters were removed.
- CLI/package contract: 38 tests passed, including packed template assets and
  immutable dependency checks. Scoped SDK/template/script ESLint and whitespace
  checks passed before the final documentation-only updates.
- Clean final-source install, clean `ci`, typed compile/production build and
  exact-lock static adoption passed. No consumer repository manifest/lock was
  upgraded. Vite reports the standard assistant bundle above its default 500 KB
  warning threshold; this is not a runtime or mobile performance qualification.
- Mills full candidate typecheck/server compile and 41 selected real gateway/
  catalog/file cases passed against the updated SDK source. The runner verified
  unchanged manifest/lock hashes. It used one worker and the established shared
  database template, then an isolated file fixture. This is source qualification,
  not a replacement for the still-failing ordinary installed-pin build.
  Log: `/tmp/mills-event-page-candidate-check.log`.

Logs: `/tmp/sdk-clean-scaffold-final-source-{install,ci,check,adoption}.log`,
`/tmp/sdk-scaffold-package-tests-complete.log`,
`/tmp/sdk-spartan-adapter-tests-complete.log`,
`/tmp/sdk-event-page-{typecheck,build,tests}.log`,
`/tmp/sdk-event-page-concurrency-{before,after}.log`,
`/tmp/sdk-scaffold-event-page-lint-complete.log`. Earlier public runtime successes
and intermittent failures remain in `/tmp/sdk-clean-scaffold-runtime-v*.log`
and `/tmp/sdk-clean-scaffold-{repeat,diagnostic}-*.log`.

### Remaining full objective

Continue generic JS/Flutter behavior and registered-consumer audits, safe
reusable-conversation Clear/reset, older unbound chat-derived retention, remaining
host composition/doc contradictions and coordinated cutover preparation. Actual
public committed pins/locks, declared Flutter dependency adoption, authenticated
web/mobile/provider/audio qualification and approved production data/storage
cleanup are still required. Preserve business/auth behavior and audit, usage and
execution/idempotency evidence. Do not mark this goal complete or claim 100%.

## Eighth autonomous turn: voice deletion and delayed activity

Reverified the same native and Handrail objectives, both active, and current
bounded working-project access. Human-answered assistant Coverage Q&A search
returned no entries. No release, dependency-pin change, project database write
or storage write occurred. This turn makes source/test progress; it is not a
blocked-audit turn and does not narrow the full objective.

Three gaps were found and corrected in local source:

- Confirmed voice hangup did not establish that an already dispatched tool had
  finished. Deletion now validates every ended call and every associated tool
  partition, including later pages, and refuses running/unknown outcomes or
  unreadable termination evidence. Canonical business execution and approval
  checks still apply; display activity is not a business-outcome ledger.
- Voice activity previously read call state before writing without the deletion
  lock. `PostgresRealtimeCallStore.withConversationLock` now provides a storage
  transaction boundary, and activity/read-acknowledgement writes recheck the call
  beneath that lock. A stale active-call read cannot recreate activity after
  hangup/deletion. Existing tool completion after hangup remains possible before
  deletion. Call/tool outcome receipts remain; presentation read acknowledgements
  are removed. No external dispatch occurs inside this storage callback.
- Mills voice records use `mills-realtime:<environment>`, separate from household
  text history. Its shared catalog callback now receives trusted authorization
  context and seals both partitions in the same SQL transaction, rechecking the
  current session policy. Active voice or unfinished tools roll back text, catalog
  and file cleanup together. Existing environment namespaces/call identities are
  preserved. A release must drain old workers that do not honor these fences.

Validation: SDK full TypeScript, normal build, scoped ESLint and 42 tests passed
(26 deletion, three voice, 13 mapped-catalog cases). Native PostgreSQL JSON
qualification plus nine independent-connection cases passed. The new native
case pauses the initial active-call read, ends/deletes through another connection,
then verifies that the delayed activity writer is refused and no activity row
appears. The owned cluster was removed. Logs:
`/tmp/sdk-voice-deletion-{typecheck-final,tests-final,build,lint,native}.log`.

Mills full candidate typecheck/server compilation passed. The initial run passed
34 gateway and 34 assistant cases; its catalog run exposed test spy state shared
between cases. After isolating that spy, the final candidate run passed all eight
catalog and 18 native live-tool cases, including rollback while voice/tools remain
active, both deletion fences, retained call receipts and blocked late admission.
Scoped ESLint and whitespace checks passed. Logs:
`/tmp/mills-voice-deletion-candidate.log`,
`/tmp/mills-voice-deletion-candidate-final.log`,
`/tmp/mills-voice-deletion-lint-final.log`.
Candidate aliases remain source qualification only. Installed dependencies and
locks are unchanged; actual committed SDK adoption and live voice/provider/audio
qualification remain outstanding. No global inventory of other environment
voice partitions or physical production removal is established by these tests.

Continue the remaining full acceptance work listed above, including reusable
Clear/reset, older unbound chat-derived retention, JS/Flutter registered consumers,
generic behavior, documentation and coordinated cutover. Source progress does not
satisfy the pending publication/adoption, authorized runtime and production cleanup
requirements. Do not mark the goal complete or claim 100%.

### Eighth-turn mobile follow-up

Clear/reset investigation found a concrete Mills mobile mismatch. The "Clear
assistant" action actually performed permanent deletion and automatically
retried a newer catalog version after conflict. The UI/repository now explicitly
name deletion and capture the reviewed ID/version. A changed version reloads for
review and requires another confirmation. A dialog cannot authorize deletion
after account replacement or permission loss.

Confirmed deletion now evicts the target's local session/projection/workspace
entry and exact saved pending intent, even when the view cancels after dispatch
or the same conversation was reselected. A local deleted-identity set rejects
delayed reads and filters stale list results. Different selected conversations
remain intact. Lost/mismatched replies retain the existing retry/validation
contract. Backend durable fences and permission checks remain authoritative.

Mills mobile scoped Dart analysis passed for the two source and four affected
test files; 130 screen/route, repository, deletion and domain-review tests passed
with one worker. This run used actual installed public Flutter pin
`d574ea74d675666ebd4ea27f0fa0a13bae2a13df`, with no package aliases or manifest/lock
changes. Logs: `/tmp/mills-mobile-delete-analyze-final.log` and
`/tmp/mills-mobile-delete-tests-final.log`. The system `dart` launcher initially
refused its unrelated `/opt/flutter` Git ownership; using the declared Handrail
Flutter SDK executable worked without changing Git trust or global configuration.
The first analysis found new formatting/type-inference lints, corrected before
the final passing analysis. No preview, provider, audio or production data work
occurred.

The shared reusable Clear contract still retains the same catalog identity and
advances its version. Current JS/Dart write requests do not carry a content-reset
generation, so deleting history then reopening that identity can admit old-tab
or delayed work. Do not enable default Clear with the permanent-deletion helper,
silently substitute a newly allocated conversation ID, or call a timestamp-only
update a reset. A safe reset needs coordinated wire/client/persistence fencing.
The default remains explicitly unsupported pending that work.

The declared Flutter SDK has shared history/controller/workspace components,
but its account controller still lacks shared permanent-deletion orchestration;
Mills still has a substantial generic history/controller shell. Consolidating
those while preserving its domain review/files/voice is independent unfinished
source work. The full JS/Flutter/consumer, retention and rollout objective remains
active; this follow-up is additional progress, not a blocked-audit turn.

## Ninth autonomous turn: shared Flutter deletion and published baseline change

Reverified the complete active native/Handrail objective and bounded-turn access.
Relevant human-answered Coverage lookup again returned no entries. This runner
performed no commits, pushes, PRs, publication, deployments or project data writes.
This is another source-progress turn, not a blocked-audit turn.

The declared Flutter SDK now has shared reviewed permanent deletion: exact
ID/version/idempotency requests, a separate account-scoped durable intent journal,
verified receipts, restart recovery and per-conversation admission guards. Lost
replies retain the exact request; version conflicts reload for another review.
An unresolved deletion does not prevent unaffected chats from opening. Verified
deletion evicts only the target's session, catalog, workspace, pending turn and
composer draft/files, and rejects delayed reads/frames. Shared sidebar and compact
history provide negotiated delete/restore/archive controls, confirmation and
pending-deletion retry. Account replacement dismisses owned dialogs and excludes
old asynchronous completions. Backend ownership, active-work and uncertain-effect
checks remain authoritative. The low-level catalog action is still available;
custom structural history bindings must supply `delete(String, int)`.

A new real HTTP gateway lost-reply/restart case exposed an older JS in-memory
receipt inconsistency: it returned the next version, whereas PostgreSQL returns
the removed descriptor version. The JS source now documents and consistently
returns `deletedVersion === expectedVersion`; the Flutter client refuses a
mismatched receipt. The default Flutter test dependency `d574ea74d675666ebd4ea27f0fa0a13bae2a13df`
therefore fails the new wire test. It has not been skipped or weakened. With an
explicit local JS build fixture override, full sequential checks pass both fatal
analysis gates, 110 client tests and 97 widget tests, including attachment and
deletion gateway integration. JS typecheck/build, scoped lint and 87 deletion/
catalog cases pass. See the Flutter SDK's `docs/conversation-deletion.md` for
contracts, exact logs and limits. These source fixtures do not establish actual
installed-consumer adoption, provider/audio execution or physical deletion.

Public repository HEADs advanced externally during this turn. Read-only local
history and public HTTPS resolution both show JS
`5a0ebe520a9e6fde0b3a792f959a7a10e0a3de50` (0.2.36) and Flutter
`c22b5ac97b0bcabd99b2d96995a0ed85c49ec124`. Earlier catalog, cleanup, scaffold,
atomic history and voice-fencing source is now in the published JS revision.
That supersedes earlier chronological statements that those APIs are unpublished.
The ninth-turn Flutter deletion and JS in-memory receipt correction remain local.
Mills web source also advanced externally; its manifest still pins `5d9387c1…`.
Reverify before adopting a frozen public SHA with a matching normal-install lock.
Do not attribute external commits/publication to this runner.

The full objective remains active: qualify clean installed JS adoption now that
the earlier baseline is available; consolidate Mills' mobile host shell; align
registered consumers with declared Flutter revisions; finish retention/reset and
cutover inventory; obtain required runtime qualification and separately authorized
release/production cleanup. No overall 100% claim is warranted.

### Ninth-turn public installation follow-up

Earlier SDK work became publicly adoptable during this turn. Mills now declares
JS SDK `5a0ebe520a9e6fde0b3a792f959a7a10e0a3de50` (0.2.36), regenerated through
the package manager with a matching lock. An initial npm 10 clean install built
successfully but rewrote the installed hidden lock to SSH. The local host now
requires the already-declared Node 22.23.1 with npm 12.0.2, allows its root Git
sources and exact prepare scripts, and uses that npm in both Docker stages.
The old Docker Git URL rewrite was removed and `.dockerignore` now admits the
required `.npmrc`. No global workspace toolchain or container/deployed runtime
was changed. Subsequent normal npm 12 `ci` compiled the SDK and preserved the
exact HTTPS resolution in manifest, root lock and installed lock.

Full ordinary Mills TypeScript checking, production client/server build, static
adoption check and scoped lint pass. The installed-SDK runs cover 254 passing
cases: 34 assistant, 34 real gateway, eight catalog/file, 18 native live-tool,
five fresh-baseline, 86 provider/approval/usage/transcription/voice/metadata and
69 client UI. An obsolete provider test fixture initially failed 12 cases because
it cast an in-memory continuation store into the complete PostgreSQL bundle.
The fixture now uses the actual SDK bundle and a typed synthetic usage sink;
all 28 provider cases and final full typecheck/lint pass. Runtime code was not
weakened to accept that obsolete fixture. The initial failed log is retained.
See Mills `docs/chat-legacy-removal.md` for exact log paths and current cutover
requirements. Current host documentation no longer instructs importing or
reconciling old chats, and active resource/API inventories omit the retired route.

The independently generated fixture `/tmp/handrail-clean-scaffold-217786b0-9-public`
installs the same frozen public SHA. Fresh install and clean `ci` both pass typed
client/server and production builds, with normal SDK prepare compilation.
The real native PostgreSQL fixture passes fresh/repeated migrations, fail-closed
auth, create/send, owner/tenant isolation, revocation, history/archive restoration
after a new server/pool/client, and queued usage recovery without a second provider
execution. All owned cluster/listener/client/pool resources were removed. The
runtime checker now checks the installed hidden lock as well as the root lock and
package version. Source syntax/scoped lint and actual fixture execution pass.
See `docs/scaffold-qualification.md` and `/tmp/sdk-public-5a-scaffold-*.log`.

This closes the earlier public-baseline and ordinary-installed-Mills-build
prerequisites, including the false history-gap race, for this frozen JS revision.
It does not publish the ninth-turn Flutter deletion/JS receipt correction, upgrade
Mills mobile or other registered consumers, build/deploy a container, execute a
live provider/audio session, or remove production data. Remaining independent
source work includes Mills' shared mobile controller/history adoption, safe
Clear/reset fencing, older unbound chat-derived retention and the full consumer/
cutover audit. Continue the complete objective; this is not a blocked-audit turn.


## Tenth autonomous turn: published Mills Flutter adoption and shared approvals

The full objective is still active. This turn performed no commits, pushes, PRs,
SDK publication, deployments or project database/storage mutations. Owned test
fixtures are separate from live data. Existing unrelated work is preserved.

Mills mobile now actually installs both assistant packages from the declared
public Flutter repository at full SHA
`c22b5ac97b0bcabd99b2d96995a0ed85c49ec124`, with a normally regenerated lock.
The already-pinned public bug-reporter SHA
`5b71b05e22a7309a13ecffec12b653a28a3afb13` supports win32 6; updating Mills'
direct package-info constraint to 10.1 resolved the remaining package graph.
No SDK aliases/overrides were used in the main checkout. Installed Git-cache
roots and HEAD match the public pin. The normal dependency graph includes
package-info 10.1, device-info 13.1 and win32 6.4. No bug-reporter source changes
or publication were needed; earlier publication-blocker notes are superseded.

Mills' custom composer TextField was replaced with the shared SDK editor and its
native context-menu hook. Two obsolete snackbar expectations now assert the
SDK's visible inline dictation errors and hit-testable Retry/Insert, preserving
recording/key and saved-transcript checks. Full analysis and 164 scoped tests
pass against the actual public installation; the initial run's two obsolete
assertions are retained in its log. The full release JavaScript web build passes.
Existing Plaid Wasm dry-run and Cupertino font warnings do not establish Wasm or
native qualification. Logs: `/tmp/mills-mobile-public-c22-{pubget,analyze,tests,screen-tests,final-analyze,web-build}.log`.

A detached source-only Mills mobile worktree was prepared from
`b9049406d954ebc4115ce13c6a7f9f882d9476c4` at
`/opt/handrail/repos/mills/mills-family-erp-v4/.mills-assistant-sdk-cleanup-217786b0`.
It contains the qualified public-baseline changes, with explicitly test-only
Dart package resolution to local SDK source. Its manifest/lock still name the
public SHA. It has not yet consolidated the host repository/controller/screen;
do not confuse this candidate with main's actual installed build. Keep the main
checkout usable until new APIs have an authorized public commit and matching
normal install. The separate older build worktree belongs to other work.

Source inspection found that the Flutter workspace exposed approval preference
but no shared pending-decision panel. Local SDK source now adds a canonical
review/decision controller and default workspace panel. It includes grouping,
exact reviewed identity/version, bounded redacted or host-validated opaque
reviews, current domain permission gates, incomplete-review refusal, expiry,
status, account/selection isolation and saved-decision recovery. Decisions are
persisted before dispatch through the existing encrypted account key-value
store, with atomic same-isolate writers, exact acknowledgement, no stored raw
arguments and a bounded journal. A competing controller adopts the first saved
choice; it cannot overwrite it. Uncertain decisions block target deletion and
opposite choices, while other chats remain usable. Confirmation does not assert
execution. Business formatting and hidden tool activity do not remove controls.
See sibling `docs/approval-decisions.md` for API and host validation duties.

Real gateway qualification exposed another shared server bug: after confirmation,
`createHandrailAssistant` fetched the mutable current proposal and returned its
later execution version on retry. Local JS now returns the exact transition
receipt captured during decision coordination. The regression confirms identical
receipt replay after fixture execution, with only one tool invocation. This
correction and the previous in-memory deletedVersion fix remain unpublished.
The new Flutter approval/deletion APIs also remain local beyond public c22.

Validation: JS full TypeScript, normal build, scoped lint and 22 assistant/
approval-coordinator tests pass (`/tmp/sdk-approval-receipt-{typecheck,build,lint,tests}.log`).
Flutter `make check` ran analyses and test suites sequentially with configured
single-worker limits: 124 client and 102 widget tests pass, zero skips, against
explicit local JS dist (`/tmp/flutter-approval-candidate-full-check.log`). This
includes 13 new client review/journal cases, five new widget/workspace cases and
a real HTTP Dart-to-JS lost-reply/restart case after synthetic execution-state
advancement. The initial wire assertion expected a gateway error; an actual
socket disconnect correctly throws http.ClientException. The corrected wire
case passes (`/tmp/flutter-approval-gateway-final-test.log`). Testing that same
case against the fixture's actual old public JS dependency fails because the
saved decision cannot settle (`/tmp/flutter-approval-installed-gateway-test.log`).
Keep that regression enabled; it records the required matching JS adoption.
An accidentally root-scoped Dart analyze traversed historical dependency source
under node_modules; the proper per-package analyses in final make check pass.
It is not a request to repair old vendored Dart sources.

Remaining independent work: adopt the shared controller/history/drafts/transcript
and new approval adapter in Mills' source candidate, preserving validated
financial review and active file/voice adapters; finish safe reset/retention and
consumer inventory; qualify remaining actual SHA/lock adoption and live web/mobile
provider/audio behavior. Publication/adoption of local SDK changes, deployment,
and approved production chat/table/storage cleanup remain separate gates. No
100% or blocked status is warranted; this was another substantive-progress turn.


## Eleventh autonomous turn: Mills repository uses the shared controller

The goal/context were reverified active and unchanged. The preceding turn was
substantive progress; there is no blocked streak. Current-scope human Coverage
Q&A search for assistant again returned no answers. No commits, pushes, PRs,
publication, deployments, project data writes or production cleanup ran.

Work is in the explicitly isolated Mills source worktree
`/opt/handrail/repos/mills/mills-family-erp-v4/.mills-assistant-sdk-cleanup-217786b0`.
The main mobile checkout remains on its qualified public c22 installation.
The candidate's generated Dart package resolution points to local sibling SDK
source, and its generated package graph/plugin metadata were prepared from the
normal main install, including the client's new crypto dependency. These are
source-test fixtures, not installed-consumer adoption. No SDK sources were copied
into the application and no path/override dependency was introduced in manifests.

`SdkAssistantRepository` now owns one `HandrailAssistantController` and delegates
catalog pagination, session ownership, new/open, durable send/retry/Stop,
permanent deletion and approval review/decisions. Its duplicate session map,
creation identity, deletion receipt/eviction and decision idempotency logic were
removed. The remaining public domain methods/projections serve the existing
screen and voice views during conversion; they must be removed when no longer
needed, not treated as the final minimal-host architecture. The SDK controller
retains creation intent, deletion fences, saved approval decisions and current
account state. New UI can bind to its public `assistant.uiBinding`.

Mills' opaque review adapter still validates the complete native response against
conversation, proposal ID/version, turn, tool call/name and argument reference.
It keeps bounded arguments and the complete flag. The current authenticated
admin/member role supplies the additional permission gate. A changed selection
or binding cannot cache a late domain review. Existing financial formatting,
protected file/dictation adapters, voice session/activity/captions and business
integrations remain active. Voice polling is updated only when its conversation
set changes, avoiding an extra voice request on each text event.

The local Flutter SDK now supports `session.waitForTurn` with optional local
observation cancellation, replacing Mills' custom completion subscription loop.
It never sends server Stop; explicit cancellation still uses the SDK's retained
identity and target. A new real HTTP test cancels the local wait, confirms the
server turn remains active and later observes its completion on the same session.
The client analysis and all 15 cross-language gateway tests pass against explicit
local JS dist (`/tmp/flutter-completion-wait-{analyze,tests}.log`). Other SDK suites
were not repeated because the source change is confined to this observer.

Qualification found and fixed a real disposal timing issue: shared polling must
stop synchronously before asynchronous voice/host cleanup. The authenticated
screen close/reopen test catches that timer leak and now passes. Another added
regression holds an older history read across a new selection; it cannot replace
the current domain view. Old fixtures were corrected to supply real canonical
lifecycle, paging order, proposal turn/tool identities and negotiated capabilities,
and approval/deletion tests use actual in-memory-backed durable storage. Runtime
validation was not weakened to accept incomplete fixtures. The former test that
reopened during an unsettled deletion now requires the shared deletion fence;
confirmation still evicts that identity.

Full Mills analysis passes (`/tmp/mills-shared-controller-verified-analyze.log`).
Across sequential source runs, 162 distinct cases pass: 104 screen cases, 57
repository/history/approval/deletion/host-operation cases, and the saved caption
panel test. Logs: `/tmp/mills-shared-controller-{tests2,tests3,regression,screen-final,voice-captions}.log`.
Initial fixture/timer failures remain in their logs; the final screen/deletion
run has 114 passing cases and includes the added overlap test. No live preview,
provider execution, financial operation, native-device or audio playback claim
is made. The SDK gateway tests use owned deterministic fixtures only.

Next required work is the Mills shared workspace/drafts/transcript/upload
conversion, preserving native clipboard/photo selection, validated financial
review and active voice controls. The current 4,000-line host screen is still
custom; this repository consolidation is an intermediate stage. SDK workspace
needs its existing composer context-menu hook exposed for Mills' iOS adapter,
and the host attachment flow needs a shared-uploader binding before deletion of
its old orchestration. Retention/reset, remaining consumer audit and actual
SHA/lock adoption, live qualification, authorized publication/deployment and
approved production cleanup remain part of the unchanged full objective.

## Twelfth autonomous turn: shared upload lifetime and Mills adapter

The native and Handrail goals/context were reverified active and unchanged.
Current-project human Coverage Q&A search for assistant returned zero answers.
This is another substantive-progress turn, with no blocked streak. No commits,
pushes, PRs, SDK publication, deployments, project storage/database mutations or
production cleanup ran. Main Mills remains on its qualified public installations;
the changes below use the isolated mobile source candidate and local sibling SDK.

The SDK draft queue now distinguishes Stop (retain the selected upload key for
retry) from removal, replacement, admission, conversation discard and account
disposal (release retained local host upload state once). Its optional
`onUploadReleased` controller/factory hook lets a host staged protocol erase its
command buffers/intent. It never implies remote file deletion. Native context
menu, paste and dictation-busy hooks now pass through the combined workspace to
the standard composer, preserving shared editor/send/focus behavior.

The candidate's `MillsAssistantAttachments` binds a SDK upload key to the original
conversation, filename, MIME type, length and content hash. It serializes retries
while an earlier transport settles, rechecks current permission, preserves the
original Mills intent/finalization identities and returns only validated SDK
attachment references. The existing protected file repository retains CSRF,
signature/dimension/checksum checks, same-origin versus signed PUT validation,
and exact finalization receipt checks. The SDK queue owns selections and status.
Opt-in retained cancellation keeps the command for retry; existing callers keep
their default local destruction. Explicit release/disposal erases retained state.

Qualification also found an existing recovery defect: after confirmed byte
upload, retrying an uncertain finalization still required the now-expired PUT
URL. Finalization recovery now uses the original identity/key without uploading
again or requiring a still-usable PUT URL. Initial PUT expiry checks remain.

Full candidate analysis and all 20 attachment repository/SDK queue cases pass,
including four new cases covering lost finalization after URL expiry, Stop/retry,
removal with a late reply, and conversation/content/permission isolation. Logs:
`/tmp/mills-sdk-upload-{tests,final-analyze}.log`. The initial ambiguous-import
analysis error was fixed with a selective photo-validation import. SDK widget
analysis passes. Across the full run and corrected workspace run, all 105 unique
widget cases pass (`/tmp/flutter-upload-release-{full-tests,workspace-verified,final-analyze}.log`).
The new native-editor test initially used an absent tooltip then tapped before
the draft rebuild; those fixture errors are corrected. The final workspace run
passes all 11 cases, including ordinary Send through the standard editor.
No browser, native device, live storage/provider or audio-playback qualification
was performed. Test HTTP boundaries use owned deterministic fixtures only.

The Mills adapter is qualified through the actual local SDK queue but remains
unwired in the custom screen. Next replace its 4,000-line generic history,
draft/upload/send and transcript orchestration with the shared workspace,
preserving native clipboard/photo inputs, saved-file display, financial review
formatting and live voice. The Mills server explicitly disables the default SDK
multipart attachment route; a workspace binding must supply the protected host
uploader and declared business limits. Do not silently point the default SDK
uploader at that absent route or treat test aliases as installed adoption.

The handoffs were updated, including correction of Mills web's stale claim that
main mobile still used the old JS-repository Dart pin. Full screen conversion,
safe reusable reset/retention, remaining consumer audit and actual dependency
adoption, runtime checks, release authorization and approved production chat,
table and storage cleanup remain outstanding. Keep this goal active.

Follow-up source inspection identified another necessary ownership change:
Mills creates `AssistantSessionScreen` inside its `/assistant` route, so closing
that route disposes the controller. Move controller and drafts to authenticated
account lifetime during the screen conversion. Existing close/reopen coverage
proves timer cleanup, not unsent-draft retention. Qualify navigation retention
and account change cleanup, including the router's lazily retained attachment
repository. The final strict UUID-reference check also passes all 20 attachment
cases again (`/tmp/mills-sdk-upload-verified-tests.log`).

## Thirteenth autonomous turn: Mills account and draft ownership

The full goal and verified context were re-read and remain active/unchanged.
The previous turn made source progress; there is no blocked streak. Current
project human Coverage Q&A search for assistant again returned zero answers.
No commits, pushes, PRs, publication, deployment or project data mutations ran.
Work remains in the isolated Mills source candidate, with explicit local SDK
resolution. The actual public main installations remain separate.

Mills' app now owns a lazy `AssistantAccountController` above navigation and
passes it to the router. The old 84-line `AssistantSessionScreen` runtime wrapper
is deleted. A route borrows the current SDK repository instead of creating and
disposing one per visit. The account owner also owns the default protected
attachment repository, so a retained router cannot carry those command buffers
into a replacement session. Revocation/replacement synchronously invalidates
local state and stops polling, then finishes asynchronous teardown. The SDK's
encrypted pending/approval/deletion journals keep their existing API/household/
user namespace and recovery semantics. A new authenticated session starts with
new local drafts even if its public user/household is unchanged.

`SdkAssistantRepository.drafts` now holds `HandrailComposerController`, linked
to `assistant.uiBinding` and the exact `beforePendingRecovery` draft acceptance
callback. The screen borrows its selected text controller; it no longer creates,
resets or disposes a separate view-owned draft. Confirmed deletion lets the SDK
discard the target draft instead of clearing whatever draft is currently selected.
The custom file selection/send/transcript presentation still needs conversion.

Full candidate Dart analysis passes. All 177 distinct cases pass in sequential
runs: 106 account/screen cases and 71 app-composition/auth-gate/repository/history/
approval/deletion cases. The navigation regression uses the real SDK controller
with deterministic HTTP responses and verifies one client/controller plus the
same unsent text after closing/reopening. Account cases cover all local drafts,
pending host upload buffers, identity/role replacement and late admission
isolation. Logs: `/tmp/mills-account-owner-{regression,app-repository-tests,final-analyze}.log`.

The initial navigation run passed retention assertions but hung while awaiting
SDK teardown inside Flutter's simulated event loop. Diagnostic runs isolated
zero-session disposal; `tester.runAsync(account.dispose)` completes it outside
that loop. The corrected complete 106-case rerun passes. Interrupted runs remain
failed diagnostics (`/tmp/mills-account-owner-tests.log`,
`/tmp/mills-account-navigation-*.log`), not successful receipts. No SDK disposal
code change or temporary diagnostic print remains.

Candidate/main qualification docs and the fixture ownership comment were updated.
The full workspace conversion remains next: bind the protected uploader and
file drafts, use the SDK history/transcript/approvals/composer, preserve native
clipboard/photo input, protected saved-file display and live voice/domain review,
then delete the superseded host orchestration. Keep account ownership outside
that view. Reset/retention, remaining consumers, actual dependency adoption and
required live qualification still remain, as do release and production-write
approval gates. This is source progress, not overall completion or rollout.

The source candidate release web build passed
(`/tmp/mills-account-owner-source-web-build.log`) with the existing Plaid Wasm
dry-run and Cupertino font warnings. It qualifies the JavaScript web build only.
Final review then removed a `??` fallback that could use an injected repository
after the account owner had been revoked. All six route cases, including a new
revoked-account isolation case, pass (`/tmp/mills-account-owner-router-final.log`),
bringing this turn to 178 distinct passing cases across its runs. The final
conditional is compiled by those tests; the web build preceded that guard.
Full final analysis is clean (`/tmp/mills-account-owner-verified-analyze.log`).
No public SDK dependency, actual consumer installation or deployment changed.

## September 14 fourteenth continuation: shared Mills workspace

Resumed the same worktree after auth rotation; verified active native and durable
goals and retained the explicit no-release constraint. Handrail-scoped Coverage
Q&A search for assistant SDK shared mobile history returned zero entries.

Mills candidate AssistantScreen now mounts HandrailAssistantWorkspace with its
account-owned drafts and SDK binding. Removed host generic catalog picker,
optimistic send state, composer, transcript cards, deletion dialog and text
approval UI. Native selection/clipboard, protected file reads, validated business
citations and the authenticated transcriber remain host adapters. Live voice
retains its saved-call/caption/domain-review surface pending further consolidation.
The account owner supplies a protected attachment provider with image/PDF bounds;
SDK ownership covers selection, upload retry/Stop/release and admission.

Flutter adds explicit attachment-provider and protected service adapters, image
limits, view-native picker and copy hooks. Four new provider/limits/scope cases
and 32 related cases pass; widget package analysis is clean. Mills' real SDK
transport navigation test passes: same account controller and unsent draft survive
close/reopen. Detailed files/logs/remaining work live in the candidate
`docs/shared-assistant-source-candidate.md`. Existing whole-screen fixtures depend
on retired repository overrides and must be ported without rebuilding a duplicate
runtime. No full screen-suite or current live/runtime qualification claim.

No commits, publication, dependency adoption beyond existing public pins,
deployments or production data writes. All wider retention/reset, consumer and
production cutover requirements remain in the original active objective.

## September 14 fifteenth continuation: shared-screen protocol qualification

Verified the native and Handrail goals are active and unchanged. Previous turn
was progress, not a blocked turn. Added Mills' reusable protocol fixture and
five actual-workspace cases for send journaling/admission/edit preservation,
canonical reload, fresh creation/new/archive/restore, viewer behavior,
authentication handoff and native PDF admission. The tests simulate terminal
saved turns and protected file responses; they do not prove provider execution.

Fixed a real integration gap: SDK catalog/session 401 errors now notify Mills'
account boundary once. Restored the image/PDF notice above the shared composer.
SDK Copy now shows an accessible bounded failure and rejects a stale failure
after a newer success. Eight transcript tests and SDK widget analysis pass.

Five retained live-voice adapter cases now use a valid catalog and explicit
account disposal; their financial review, captions, uncertain end, saved tool
activity and speaker assertions pass. An additional account-replacement case
verifies modal/media cleanup, and navigation retention still passes. These
are mocks, not audio/provider qualification. Full Mills analysis passed. The
combined seven voice/navigation cases and 62 workspace/domain regression cases
pass. Detailed log paths and next work are in the Mills candidate handoff.

Remaining whole-screen fixture conversion is explicit; preserve native
clipboard, dictation/retry/cancellation, auth, financial review, history/deletion
and accessibility semantics while eliminating old host override assumptions.
The complete original objective, publication/pin adoption, live checks and
production cleanup remain outstanding. No commits, releases or data writes.

## September 14 sixteenth continuation: native input boundaries

Verified current native/durable goals remain active and unchanged. Prior turn
was progress. Ported five dictation tests to shared controller/draft behavior
with only protected transcription mocked; the combined 12 voice/dictation/
navigation cases pass. Added two actual-workspace deletion review cases for
explicit confirmation and a stale catalog version, plus a native clipboard
channel case covering conversation/account changes. The eight-case workspace
protocol suite and full Mills Dart analysis pass.

The iOS boolean-only image-paste bridge lacked conversation correlation. Its
paired Dart/Swift replacement carries a bounded local context, checks it before
forwarding and admission, clears rejected Dart payloads, and tracks which screen
owns the method-channel handler. This is prepared source, not native runtime
proof: swiftc/xcrun are absent, so AppDelegate.swift compilation/UIKit behavior
and a matching iOS binary rebuild remain pending. No workaround or raw mobile
preview listener was used. Detailed logs and next work are in the Mills candidate
handoff. No SDK commits/publication, dependency adoption, releases, deployment
or production database/storage writes. The complete original goal stays open.

## September 14 seventeenth continuation: financial review and shared bundle

Verified active unchanged native/durable goals. Prior turn made progress; no
blocked streak. Relevant Handrail Coverage Q&A query returned zero entries.
Nine actual-workspace financial approval cases replace five host-only proposal
fixtures, covering complete/current review, opaque retry, explicit decisions,
revoked/viewer permission, changed versions, late-account results and exact
saved-choice recovery. Generic approval controls remain visible independently
of tool-activity styling. Added SDK approvalTitle/titleFor display hooks so
Mills can retain business-friendly action labels without a decision runtime.

34 workspace/financial repository cases, 12 voice/dictation/navigation cases,
15 SDK approval/workspace cases and Mills/SDK-widget analyses pass. The current
Mills shared-screen candidate passes release web compilation in 64.3s. Existing
Plaid Wasm and Cupertino-font warnings remain; local aliases establish source
compilation only. Detailed logs/remaining work are in the Mills candidate handoff.

Remaining full screen fixture conversion and the broader original SDK/consumer/
retention objective remain open. Swift compilation/native execution, actual new
public dependency adoption, live provider/audio parity and production cleanup
are unverified. No commit, push, publication, PR, deploy or production write.


## September 14 eighteenth continuation: voice history and citation boundaries

Resumed after account rotation without resetting the task. Verified active,
unchanged native and durable goals and current action-specific access. Handrail
Coverage Q&A search for voice/history/unread returned no owner answers. Progress
continued; no blocked streak and no goal objective/status mutation.

Moved voice workspace observation ownership from Mills into the shared Flutter
assistant controller, using a protected host reader. Shared history now retains
separate active/unconfirmed/unread/unsettled voice markers and bounded offline
retry. Text selection/read never acknowledges voice, and voice state never
changes text send/Stop permissions. Tests cover unchanged-catalog read coalescing,
archived/background rows, deletion eviction, late/disposed replies and auth expiry.

Fixed a shared citation navigation bypass: a configured resolver returning null
now vetoes the raw locator fallback. Added explicit allowMessageLinks policy;
Mills keeps raw Markdown links inert and navigates only its validated summary/
connections sources. A real GoRouter test covers both allowed destinations and
rejected internal/external/wrong-identity sources. Non-assistant text stays literal.
Replaced the obsolete host-only voice-history and citation fixtures after those
stronger shared SDK/transport/router checks passed.

54 unique SDK tests and 41 Mills cases pass in sequential scoped runs, plus
SDK client/widgets and full Mills analysis. Exact logs and remaining fixture
conversion are in Mills' docs/shared-assistant-source-candidate.md. The turn-17
release web source bundle remains the latest build receipt and predates these
hooks; no native/provider/audio/public adoption claim follows from these tests.

Remaining independent work includes old screen HTML/formatting/native-file tests
and removal of superseded host compatibility code, plus the wider original
retention/reset and scoped-consumer inventory. No commits, pushes, PRs, SDK
publication, deployment or production database/storage writes occurred. New
public SHA/lock adoption, Apple/native qualification, live parity and approved
production cutover still prevent overall completion.


## September 14 nineteenth continuation: shared protected images and app routes

Verified the unchanged active goal, current action-specific access and prior-turn
progress. Relevant Handrail Q&A query returned no answers. Removed Mills' separate
image preview/modal lifecycle in favor of the shared SDK protected image viewer.
The SDK now provides optional scoped zoom, fresh authorized activation, bounded
failure/retry, cancellation, owned-route closure, buffer/cache cleanup and
background/resume handling. Mills supplies current-account/conversation checks
and its existing strictly validated private-image reader. PDF references remain
metadata-only; unsupported image dimensions no longer render as null labels.

Twenty-one SDK image/saved-file cases and 21 Mills cases pass sequentially, plus
SDK-widget/full Mills analysis. Six real app-route cases now use the canonical
gateway fixture with explicit account disposal. Removed two superseded host-only
image/PDF fixtures after stronger shared SDK/protected-HTTP coverage passed.
Full evidence/logs and remaining work are in Mills' candidate handoff.

The original objective is still open. Next independent work includes the rest
of the old screen formatting/copy/keyboard/native-source fixture conversion,
removal of unused host constructor/runtime compatibility seams, and the broader
safe reset/retention and registered consumer audit. No release or production
action occurred; source aliases remain separate from actual public dependency
adoption. Native/live provider/audio qualification and approved production
cleanup are still pending. No blocked streak or goal status/objective change.


## September 14 twentieth continuation: catalog permission and recovery

Verified unchanged active native/durable goal, current access and prior-turn
progress. Handrail Q&A lookup for viewer/conversation permission returned zero
answers. Mills server catalog source already revalidates session/role/expiry;
its shared client had omitted the matching host permission signal.

Added Flutter `allowConversationManagement` and wired Mills' existing live
admin/member gate. Read-only history remains available without auto-creation;
New/title/archive/restore/delete honor the gate and current confirmations recheck
it. Revocation during deletion journaling prevents dispatch while preserving the
exact saved intent. Read-only restart leaves that identity pending and opens
other history; restored permission resumes the same ID/version/key. The server
still authorizes every request, including changes while HTTP is in flight.

Early permission failures exposed uninitialized/stuck operation tracking. New,
lifecycle and deletion work now starts after its operation future is registered.
32 client catalog/deletion/permission tests and 23 history/workspace widget tests
pass, along with 13 Mills permission/workspace/account and 18 retained native/
route cases. All three scoped analyses pass. Full logs are in Mills' source
candidate handoff. Five obsolete host-only history/deletion tests were removed
only after replacement coverage passed.

Removed unused screen/router attachment-repository and signed-uploader injection
and the duplicate router factory; the active account-owned SDK upload provider
and protected business/file transports remain. Other old formatting/copy/input
fixtures and compatibility methods still require conversion/removal. Continue
the original retention/consumer audit, verifying reset requirements against the
agreed standard rather than treating every obsolete unsupported API as a feature
to implement. No releases or production data writes; public adoption, native/live
qualification and coordinated approved production cleanup remain open. No blocker
streak or goal objective/status change.


## Twenty-first continuation: actual Mills rendering/input and retired fixtures

Resumed the existing source candidate after account rotation, preserving prior
work. Verified current unchanged active native/durable goals and access scope.
Handrail Coverage Q&A for mobile assistant rendering/attachments returned zero
answers. No blocked streak.

Mills now has real SDK workspace tests with its app theme for Markdown emphasis,
narrow table scrolling, exact original Copy/bounded failure/retry, literal sent
text, internal-message omission and inert links/remote images. Native Photos,
Camera and Files feed SDK drafts without sending; protected selection bytes are
released and late results cannot cross conversation/account changes. Keyboard,
safe-area, focus, latest-message and Send behavior pass at 320/402 widths. Mock
terminal admissions do not establish provider execution or native plugin behavior.

Fixed SDK reusable message role labels: system/tool content is literal, accurately
announced and visibly identified even with normal author labels hidden; assistant
branding/avatar are not assigned to those roles. Standard chat still filters out
internal messages. Shared composer defaults now inherit host surface/text/border/
primary colors and font, including dark themes. Explicit overrides and standard
features remain. Hosts no longer need duplicate composer code to avoid forced
white/dark/green styling.

Removed 12 obsolete host screen cases after corresponding SDK/actual-host
coverage, plus dead formatting/navigation/source-picker/citation helpers.
Removed `AssistantReadRepository`, `ReadRepositories.assistant`, the unused SDK
read adapter, synthetic chats/proposals/quick prompts and their dead model family.
Household/calendar/finance fixtures and active domain review/file integrations
remain. Updated affected test constructors, README and the specific mock adoption
inventory entry; no silent claim that the entire old screen suite now passes.

Validation: 43 shared SDK widget tests and 66 distinct Mills tests pass. SDK
widgets and full candidate analysis pass. Exact commands, logs, replacement
mapping and limits are in Mills `docs/shared-assistant-source-candidate.md`,
section twenty-first continuation. SDK logs:
`/tmp/flutter-shared-ui-turn21-final.log`,
`/tmp/flutter-shared-ui-turn21-analyze.log`. Mills logs:
`/tmp/mills-workspace-turn21-final.log`,
`/tmp/mills-read-boundary-turn21.log`,
`/tmp/mills-theme-input-turn21-final.log`,
`/tmp/mills-native-routes-turn21-final.log`,
`/tmp/mills-workspace-turn21-analyze-final.log`.

Continue old generic send/photo/failure fixture and compatibility-method removal,
remaining generic voice consolidation, retention/reset requirement review and
registered-consumer audit. Only SDK client/widgets resolve to local Flutter
source for candidate qualification; manifests/lock retain public c22. Actual
public SDK adoption, native/live audio/provider checks and coordinated approved
production cleanup remain pending. Last source release web bundle is turn 17,
before later source changes. No release actions or production SQL/storage writes
were performed. Goal stays active; overall completion is not established.


## Twenty-second continuation: remove generic Mills compatibility runtime

Verified unchanged active goals/current checkouts. Prior turn was progress;
Handrail Coverage Q&A for assistant send/retry/cancellation returned zero answers.
No blocked streak.

Removed Mills' unused generic catalog/list/create/delete/send/attachment-send/
retry/Stop methods, their completion-result assembly and legacy reply/catalog
models. Live UI already used the SDK directly. The host repository now retains
only protected file/transcription, voice/caption, financial review and domain
projection work; it is 865 lines rather than 1144 at turn start. Removed 36 obsolete
generic test declarations and fake runtime/upload helpers after mapping their
replacement coverage. The entire retained screen test file now passes its 18
native-adapter/app-route cases, alongside the actual SDK workspace suites.

Voice review had used a helper that selected/opened a chat, retried saved sends
and marked text read. New `readConversationProjection` refreshes authorized domain
data without those side effects. A regression verifies selection, two unread
records and another chat's exact pending intent survive review reads, with no
mutation requests. Existing delayed-read/deletion/account fences still pass.
Direct SDK deletion now clears Mills cached review arguments from the SDK change
subscription, independent of any removed host delete wrapper.

Moved uncertain local-journal acknowledgement coverage to the real JS/Dart SDK
gateway: a write that persists but throws before acknowledgement admits nothing;
a different draft cannot replace it; restart recovers the saved request/turn with
one deterministic provider invocation. Used explicit local JS dist for source
qualification, not an installed-consumer adoption claim.

Validation: 137 distinct Mills cases and 52 SDK cases pass across sequential
runs. The first converted host run's six expectation failures and the first new
SDK test's compile error were corrected and rechecked; exact logs and coverage
mapping are in Mills `docs/shared-assistant-source-candidate.md` section 22.
Full candidate analysis and SDK-client lib/test analysis pass. New source
`flutter build web --release --no-pub` passes in 72.7 seconds; existing Plaid 5.2.0
Wasm dry-run/Cupertino font warnings remain. Inspected compiler memory while
running; compilation finished normally. This is a JS release web source bundle,
not Wasm/native or live audio/provider qualification. All test/build handles ended.

Evidence: `/tmp/mills-compat-removal-turn22.log`,
`/tmp/mills-compat-removal-turn22-retry.log`,
`/tmp/mills-sdk-workspace-turn22.log`,
`/tmp/mills-compat-removal-turn22-analyze-final.log`,
`/tmp/mills-sdk-cleanup-turn22-web-build.log`,
`/tmp/flutter-draft-retirement-turn22.log`,
`/tmp/flutter-submission-retirement-turn22.log`,
`/tmp/flutter-submission-retirement-turn22-final.log`,
`/tmp/flutter-client-retirement-turn22-analyze.log`.

Continue generic native voice consolidation, retention/reset requirement review,
registered-consumer audit and the precise runtime/cutover blockers. Candidate
aliases/public c22 pins and locks are unchanged. No commits, pushes, PRs, SDK
publication, deployment or production SQL/storage writes. Actual SDK adoption,
required native/live checks and production cleanup still prevent overall 100%.
The complete original goal remains active.

## September 14 twenty-third autonomous continuation

Resumed the same active native/durable goal after account rotation and preserved
all existing work. Added `HandrailRealtimeVoiceSurface<T>`, its shared phase/state
and trusted-session contract to Flutter widgets. Mills now supplies saved-call
preflight, immutable speaker choices, captions/activity and financial review;
the SDK owns themed status, startup/retry, mute/playback, Stop/Back, uncertain-end
recovery and background stopping. Generic controls remain present with custom
branding and host review. Mills' screen shrank from 1,893 to 1,588 lines in this
continuation; its native media transport is still a host adapter.

Startup preflight is fenced after close, account/conversation replacement,
permission change and backgrounding. Active calls stop on leaving foreground and
never restart automatically. Close requires a confirmed end; explicit leave
preserves an unconfirmed result. Mills' native adapter joins concurrent
Stop/disposal, disables/releases late microphone and stale remote tracks, and
continues cleanup if playback teardown throws. A cancelled financial read no
longer prevents fresh review after an uncertain end, and its late result cannot
replace newer proposals.

Qualification: 12 SDK widget cases and 24 distinct Mills cases pass. The latter
combine the final 19-case native/route/review suite with the two native adapter,
one playback and two SDK voice-history cases. SDK-widget and full candidate
analysis pass. The final release web build passes in 69.1s with the existing
Plaid Wasm dry-run and Cupertino-font warnings. This is a JavaScript web build,
not Wasm/native qualification. Logs:
`/tmp/flutter-voice-turn23-tests-final.log`,
`/tmp/flutter-voice-turn23-analyze-final.log`,
`/tmp/mills-voice-turn23-tests-final.log` (23 overlapping cases),
`/tmp/mills-voice-review-turn23-tests.log` (19 cases including the added review
regression), `/tmp/mills-voice-turn23-analyze-final.log` and
`/tmp/mills-voice-turn23-web-build.log`. Initial compilation needed an explicit
ValueNotifier type in the test and a ValueListenable import in the SDK; the
native fixture's dynamic argument access was also typed before final analysis.

All checks are local source/owned platform-boundary fixtures. No SDK publication,
SHA/lock adoption, commit, deployment, remote chat/file deletion or provider/audio
qualification occurred. The generic WebRTC transport/playback engine remains to
be extracted; retention/consumer audit, current public adoption, native/live
qualification and approved production cleanup remain open. See the Flutter
sibling `docs/realtime-voice-surface.md` for the supported contract and limits.

## September 14 twenty-fourth autonomous continuation

The prior turn was verified source progress, and native/durable goals remain
active with the exact objective. Scoped human Coverage Q&A for realtime returned
no entries. No release or database authorization was inferred.

Extracted Mills' generic WebRTC engine and native/browser playback into the
Flutter widgets package as `HandrailWebRtcVoiceSession<T>`. The SDK now owns
capture, peer/data-channel setup, ICE readiness, SDP application, media events,
mute/playback, stable call identity, cancellation and teardown. The trusted
`HandrailRealtimeVoiceGateway<T>` supplies authenticated SDP exchange and exact
end receipts. Mills' adapter shrank from 615 to 95 lines and no longer imports
WebRTC or contains playback implementations; speaker context and auth handling
remain host-owned. Its generic saved-call/activity presentation and business
caption/review adapters remain to audit/consolidate.

The SDK declares the media libraries already installed by Mills (`flutter_webrtc`
1.5.2, `dart_webrtc` 1.8.1 and `web` 1.1.1 resolved) and updates its normal lock.
A normal offline pub get succeeds using owned cached dependencies, without SDK
packaging/publication. The Mills public SDK pin/lock is unchanged; its explicit
source alias remains qualification scaffolding. Its now-redundant direct WebRTC
dependency entries can be removed only when a published new SDK pin and normal
host lock supply the transitive packages. This is not completed public adoption.

Qualification: 20 SDK cases pass (12 voice surface, two delayed-capture/disposal,
one native playback and five full WebRTC Dart/platform-channel gateway cases).
They exercise exact call identity, mute/Stop/end retry, late SDP cancellation,
unknown bootstrap results, correlated provider events and redaction. Ten Mills
protected gateway cases and the 19-case native/route/review suite pass, for 29
Mills cases. The initial two gateway failures were test envelopes missing the
required requestId; fixtures now use the actual API contract. No error parsing
was loosened. Full SDK-widget and Mills analysis pass. The release JavaScript
web build passes in 71.6s with the existing Plaid Wasm dry-run and Cupertino-font
warnings; it is not Wasm/native qualification. Logs:
`/tmp/flutter-voice-turn24-pub-get.log`,
`/tmp/flutter-voice-turn24-tests.log`,
`/tmp/flutter-voice-gateway-turn24-tests.log`,
`/tmp/flutter-voice-turn24-analyze-final.log`,
`/tmp/mills-voice-turn24-tests.log` (19 screen cases plus eight passing gateway
cases before the two fixture corrections),
`/tmp/mills-voice-adapter-turn24-tests-final.log` (all ten gateway cases),
`/tmp/mills-voice-turn24-analyze-final.log`,
`/tmp/mills-voice-turn24-web-build.log`.

No actual microphone/provider/audio execution or iOS build is established by
these fixtures. Retention and remaining consumer/presentation audits, actual
public SHA adoption, authorized runtime qualification and approved production
cleanup remain open. No commits, pushes, PRs, SDK publication, deployments or
remote DB/storage writes occurred. The authoritative media contract is in the
Flutter sibling `docs/realtime-voice-surface.md`.

## Twenty-fifth autonomous turn: Cents fresh-database baseline

Resumed after the auth/usage rotation without restarting prior work. Verified
current access and both active goal objectives. The default project-scoped
human-answered Coverage Q&A search for assistant migrations returned no entries;
that is not evidence of answers from other projects. No release or production
write was attempted, and this turn makes substantive progress rather than adding
to a blocked streak.

Cents' normal migration entrypoint now installs an empty-database baseline
through the immutable 96-migration prefix ending `0095_server_apt_checks`.
The baseline excludes `hitcents_erp_app.cents_conversations`, `cents_messages`,
`cents_tool_invocations` and their five functions. It preserves the retained
business schema, two seeded roles and the Pulse scheduler job using the original
seed SQL, including installation-time timestamps and Chicago scheduling.
Checksum/prefix/seed validation precedes one transaction containing the schema,
ledger prefix and explicit `drizzle.__cents_baselines` origin marker. Existing
schemas skip this path. The ordinary financial and assistant SDK migrations
remain after the application migration boundary.

Generated the current Drizzle model snapshot in an isolated temporary folder and
replaced its generated SQL with the reviewed non-mutating `0096` boundary before
copying anything into the repository. Prior SQL, journal entries and snapshots
were preserved. The snapshot test proves a subsequent model generation produces
no changes; the full historical ledger still intentionally recreates its original
empty legacy structures when directly replayed.

Validation: seven new baseline cases plus ten existing migration cases pass.
Native PostgreSQL 15 qualification on three owned disposable databases passed
through development and compiled production migration entrypoints: 97 ledger
entries after fresh install/replay, assistant SDK schema present, three retired
tables omitted on fresh, historical tables and pre-source public role data
preserved. CHECKs and partial indexes are round-tripped through PostgreSQL for
semantic comparison; the initial index difference was caused by reparsing the
dump's explicit array cast, not a changed predicate. Tools typecheck, scoped
ESLint and normal server compilation passed. The fixtures were closed and removed.
Cents `docs/fresh-database-baseline.md` contains the commands, maintenance contract
and `/tmp/cents-baseline-turn25-*` evidence. The host cleanup/integration guides
and shared adoption standard now describe the actual fresh/existing split.

Remaining work remains material: Spartan's baseline; source audits and actual
public dependency adoption for Cents/Spartan web/mobile; generic wrapper/UI
cleanup; retention of older unbound provider records; unpublished JS/Flutter
feature adoption; native/live provider/voice and production cutover qualification.
Cents/Spartan still pin public JS `5d9387...` locally, and their registered mobile
pins remain older than Mills' public Flutter `c22b5a...`. No pin changed this turn.
The next baseline target, Spartan, has 141 journal entries ending
`0140_administrator_payment_application_corrections`, with model snapshots only
through `0049`. Its `runOrderedDatabaseMigrations` and historical hash validator
must remain compatible with any fresh-only baseline; no Spartan source changed
in this turn.
Cents mobile's header says `Clear` but opens an explicit start-new conversation
confirmation and calls `newConversation`; it duplicates the shared workspace's
New action. Audit/remove that misleading duplicate as part of the host UI cleanup.
Do not infer a requirement to reset and reuse a deleted conversation ID from old
progress prose: the agreed objective requires safe deletion/retention and complete
standard features, while the default SDK correctly advertises unsupported clear.
Actual adoption, production data removal and live qualification remain unproven;
the overall goal is not complete.

## Twenty-sixth autonomous turn: Spartan fresh-database baseline

Verified the current access result and both active goals. The prior Cents turn
was verified progress, with no blocked streak. The default Handrail-scoped human
Coverage Q&A search for Spartan assistant decisions returned no entries; no
cross-project answer or access rejection was inferred. Preserved the existing
unrelated `.credit-patch-uZvSzY/` work in the Spartan checkout.

Spartan now installs its fresh baseline before ordinary application migrations
inside the existing `runOrderedDatabaseMigrations` boundary. The unchanged
historical hash validator still gates financial SDK migrations and readiness.
The baseline binds 141 original SQL hashes/timestamps through
`0140_administrator_payment_application_corrections`; schema, ledger prefix and
`drizzle.__spartan_baselines` origin are atomic. The frozen chain leaves zero
seed rows on an empty database. The generator verifies that fact, unexpected
schemas/functions and business dependencies, then removes only five Aegis tables
in its disposable PostgreSQL cluster using RESTRICT. The resulting baseline has
139 retained tables across `spartan` and `public`, including the seven assistant
SDK tables. Existing schemas skip it and preserve the original migration path.

The current snapshot includes the database, record-attachment and billable-expense
models through Drizzle's exported snapshot API. The `0141` follow-up is only
`SELECT 1`; no generated destructive DDL was adopted. All original 141 SQL files,
journal entries and historical snapshots were compared against Git and remain
unchanged. New ordinary model generation produces no changes. The generator's
public namespace declaration is idempotent because PostgreSQL already creates
an empty public namespace; the installer still refuses existing public objects.

Validation passed 22 distinct cases: seven baseline cases, two migration-history
validator cases, one installed-SDK schema case and 12 native assistant composition
cases. The native composition fixture now starts from the real fresh baseline
instead of creating and dropping legacy tables. It preserves review/confirmation,
role/membership/session/principal/company reauthorization, pending-turn isolation,
Handrail domain session lifetime, 150/151-tool budgets and transcription replay.
The first baseline comparison run used the Cents fixture's search_path and placed
SDK tables under Spartan; the test setup was corrected to the actual host's
public SDK schema. Runtime source was not loosened to accommodate the fixture.

Native PostgreSQL 15 qualification passed on three disposable databases through
both development and compiled production commands. The historical 141-entry
prefix remained exact, the new boundary produced 142 total entries, and a stored
SDK execution receipt survived. Fresh install/repeat contained no retired Aegis
tables, seven public SDK tables and healthy financial migrations. Pre-source
public role data remained intact. All owned pools/clusters were closed/removed.
The installed migration path emits a node-postgres concurrent-client-
query deprecation warning, but commands and readiness checks pass with current
pg; this is recorded in the host qualification guide rather than hidden.

Normal server compilation, scoped TypeScript for the baseline and native tests,
scoped ESLint and whitespace checks passed. Evidence and maintenance commands
are in Spartan `docs/fresh-database-baseline.md`, with logs prefixed
`/tmp/spartan-baseline-turn26-`. Host cleanup/integration guides and the shared
adoption/progress documents now reflect all three locally qualified baselines.

Next work is consumer feature/adoption cleanup and older unbound retention, not
another fresh-schema implementation. Cents/Spartan public JS pins remain 5d9387;
registered mobile pins remain 908e8e/933cf3, while Mills uses public JS 5a0ebe and
Flutter c22b5a. Newer local SDK approval/deletion/voice work still lacks a public
committed revision. Cents' duplicate Clear action maps to an explicitly confirmed
New conversation and should be consolidated into the shared workspace. Audit
actual callers instead of treating old prose about reusable clear/reset as a new
requirement. Live provider/audio qualification, actual final SDK adoption and
production cutover remain incomplete. No commits, pushes, PRs, publication,
deployments or production writes were performed. The overall goal stays active.

## September 14 twenty-seventh autonomous continuation

Resumed the existing Cents mobile work after the usage/auth rotation. Verified
current context, native goal and durable Handrail row remain active with the same
objective. The bounded goal turn reports `verified_owner=false` and
`execution_policy=existing_action_specific_authorization`; this is not an actual
project access rejection. A scoped human-answered Coverage Q&A search for Cents
returned no entries. Continued local action authorization; no release or
production write was performed. This is substantive progress, not a blocked turn.

### Real Cents mobile installation and cleanup

Repository: `/opt/handrail/repos/hitcents/hitcents-erp/hitcents-erp-mobile`.
Verified public Flutter main still resolves to
`c22b5ac97b0bcabd99b2d96995a0ed85c49ec124`. Changed both assistant pins from
`908e8e1ed4db3e9ded0e6464bdb98e59e0de3cbf` to c22 through normal `flutter pub get`
and its matching lock. Client 0.1.5 becomes 0.1.7; widgets remain version 0.1.1 at
the new SHA. Both installed package URIs point at the exact public Git cache.
Bug reporter remains public `5b71b05e22a7309a13ecffec12b653a28a3afb13`.
No runtime SDK alias, package override or copied SDK was installed.

`CentsSdk` no longer extends ChangeNotifier or forwards generic SDK operations;
it exposes the actual assistant and composer controllers. It retains protected
HTTP, current-account encrypted pending storage, route/business request creation,
original draft acceptance and disposal. Removed sheet-level duplicate initialize,
change forwarding and listeners. The shared workspace owns those lifecycles.
Removed host Clear (which only created another conversation), its confirmation
sheet and obsolete import-attribution rendering branch. Shared New preserves
older conversations for later reopening. Existing domain result charts/actions,
trusted route context and server authorization remain. The three Cents source
files total 918 lines (SDK 169, sheet 198, presentation 551).

The host supplies composer surface/onSurface colors through existing SDK theme
options, fixing the dark theme's white composer without suppressing attachments,
approval settings or dictation. Six goldens cover populated/error/New in both
light and dark. Removed only the obsolete current-SDK Clear-confirmation goldens;
pre-SDK historical authority/comparison artifacts remain. Tests assert the whole
negotiated toolbar is reachable. The archived-history fixture now advertises the
archive/restore routes it actually implements; fail-closed capability behavior is
preserved. Removed one redundant foundation import in the native auth test to
clear the full app analyzer; no auth behavior changed.

Normal installed validation:

- Combined SDK/sheet/alias/contract/golden/route-matrix/reference run: **94 pass,
  2 fail**. Both failures are the current Cents image comparison:
  **0.142436092633 > 0.123000**. No bound increase, mask, hidden feature or changed
  authority image. Six active golden comparisons pass; that is not design parity.
- Full Flutter app analysis: no issues. Normal release JavaScript web build:
  success in 58.7s. Optional Wasm dry run reports the existing Plaid JS interop
  incompatibility; no Wasm claim is made.
- Normal installed archive/filter/restore test passes again after correcting the
  capability fixture. All expensive checks use one worker and run sequentially.

Logs: `/tmp/cents-mobile-turn27-pub-get.log`,
`/tmp/cents-mobile-turn27-qualification.log`,
`/tmp/cents-mobile-turn27-analysis-final.log`,
`/tmp/cents-mobile-turn27-web-build.log`,
`/tmp/cents-mobile-turn27-public-archive-final.log`.

### Separate shared source presentation follow-up

The newly installed SDK puts Copy within each colored message bubble. In the
local Flutter sibling, `conversation_transcript.dart` now places Copy below the
bubble at the trailing edge of the bounded message region, with surface contrast
and the existing full touch target. Copy remains standard and retains exact-text,
accessible failure/retry, stale-completion and account-isolation behavior. The
message width bounds action/error presentation too. This is a local shared source
change, not a patch of the installed public dependency.

SDK transcript suite: **13 pass**; scoped fatal-info analyzer: no issues. Actual
Cents source integration against an explicit temporary package graph: **42 pass**,
including its six populated/error/New goldens, copy/citations, archived history,
drafts, uploads, dictation, pending identity and protected account routes. Its
visual metric is **0.123638509789**, improved but still above **0.123000**. The
first layout experiment measured 0.123946137308; neither has been called a passing
visual qualification. Manual review covered the latest source light/dark images.

Source-only graph:
`/tmp/cents-mobile-turn27-source/.dart_tool/package_config.json` plus its native
assets package graph/pubspec handoff. It maps only the two SDK packages to the
local sibling and includes needed dependency metadata. Early scratch attempts
were missing package_graph/pubspec metadata, then the old archive fixture omitted
capabilities; those setup/test issues were corrected before the qualified run.
Normal app package resolution never changed. All six active app golden files were
restored to the actual public install after temporary candidate comparisons.
Candidate images and logs: `/tmp/cents-mobile-turn27-source-goldens/`, especially
`generation-qualified.log` (42 pass) and `visual-reference-qualified.log` (11 pass,
1 visual failure). SDK logs:
`/tmp/flutter-transcript-turn27-actions-tests-final.log`,
`/tmp/flutter-transcript-turn27-actions-analysis-final.log`.

Updated Cents mobile's stale candidate-only report, both Cents web cleanup/adoption
reports, its visual manifest's obsolete metric narrative, Flutter adoption and the
shared adoption standard. The visual maximum/history is unchanged and continues
to fail against the actual public install. Earlier untracked Cents comparison
failure PNGs were moved to `/tmp/cents-mobile-turn27-earlier-golden-failures/`.

### Next unfinished work

1. Finish the Cents visual requirement without hiding SDK features or weakening
   its reference gate. The shared source improvement still misses by 0.00063851;
   installed c22 remains farther away. Keep actual/source image evidence distinct.
2. Inspect and complete Spartan mobile's remaining generic wrapper/presentation
   cleanup and public c22 installation. It was still pinned to public 933cf303 at
   the last inspection; reverify before changing. Cents and Spartan web still need
   qualified adoption from JS 5d9387 to the agreed public 5a0ebe baseline.
3. Continue remaining registered-consumer, retention/cutover and saved voice
   presentation audits. New local deletion/approval/voice SDK additions exceed
   published Flutter c22/JS 5a0ebe. Actual adoption requires authorized public
   release and matching locks; these source tests do not satisfy that boundary.
4. Required live/native/provider/audio and coordinated production cleanup remain.
   No attempt in this turn establishes a platform block; use authorized routes
   when ready. Production SQL/storage writes and release actions were not made.

No running qualification process remains at the handoff. Keep both goals active;
overall completion and a three-turn repeated blocker are not established.

## September 14 twenty-eighth autonomous continuation

Verified native goal, durable Handrail row and current context remain active with
the exact full objective. Bounded access remains `existing_action_specific_authorization`;
no release or production write is authorized by the objective. Human-answered
Coverage Q&A search for Aegis returned no entries in the available current scope.
The preceding Cents turn was progress. This turn also makes substantive progress;
a global three-turn blocked streak is not established.

### Spartan installed baseline and source cleanup

Repository: `/opt/handrail/repos/spartan-cyber-services/spartan-cyber-erp-v3/scs-erp-mobile`.
It began clean; no applicable AGENTS file was found. Public Flutter main was
verified at `c22b5ac97b0bcabd99b2d96995a0ed85c49ec124`. Normal public HTTPS Git
installation changed both assistant packages from
`933cf303bc3acaec68ed3e31f1548d3ed87a443e` to c22 with the matching normal lock.
Client 0.1.4 becomes 0.1.7; widgets remain 0.1.1. Exactly two dependencies change;
app version 0.1.56+1 and all other package versions remain. Both actual package
URIs resolve the exact c22 Git cache. No alias, override, edited installed SDK,
local copied SDK or separate packaging step was used.

The initial unchanged host baseline passed 56 Aegis cases. Source inspection
confirmed the shared workspace/controller migration already existed, but an unused
second copy of catalog/filter, title/preview, messages/citations and selected-thread
workspace data remained. Removed `AegisCatalogEntry`, `AegisMessage`, message/source
roles and source DTOs, `AegisThreadSummary`, `AegisThreadTitle`, `AegisSelectedThread`,
`AegisWorkspace`, `AegisWorkspaceView` and unused upload-progress typedef. Removed
their duplicate catalog mapping, message/preview/citation projection and refresh
path. SDK generic workspace state remains intact.

`AegisConversationActions` now contains only conversation identity and domain
pending/completed action cards. `AegisActionProjection` consumes the SDK's canonical
descriptor/document for exact tool/turn/proposal result matching. The repository
publishes only the current selection/revision, clears review metadata on selection
change and drops late failures as well as late successes from old conversations.
Confirmation refresh joins that same projection task instead of starting a second
mapping request. Current financial review, argument/version matching, stable
transition keys, encrypted account transport and protected downloads remain.
New delayed-success/failure tests verify old cards/approvals cannot replace or
block the newly selected conversation.

The deleted host citation converter was not connected to the actual shared
transcript. Added the correct `citationLink` seam using Spartan's current URL
validation. Actual UI coverage now activates internal `spartan-erp:` citations,
opens a validated external source and keeps unsafe locators inert. Retired tests
of the unused second citation DTO were replaced by this real integration test.
SDK Markdown, text, files, Copy, history, approvals and activity stay available.

### Real theme and validation

Aegis supplies surface/onSurface composer colors through existing c22 SDK options.
Its normal complete toolbar remains. Tests now use `spartanTheme`, and the reusable
`tool/capture_aegis_presentation_test.dart` runs six offline captures with toolchain
Roboto/MaterialIcons at 320×568 with doubled text, 390×844 and 1000×760 in both
themes. It asserts reachable attachment, approval, microphone and Send controls
and a present financial confirmation card. This exposed a real 21-pixel overflow
at `aegis_conversation_screen.dart`'s action/status Row. Replaced that header with
a Wrap so status remains readable without shrinking text or hiding information.
All six final captures pass; reviewed phone light/dark, small dark and tablet light.
Artifacts: `/tmp/spartan-aegis-presentation/{small,phone,tablet}-{light,dark}.png`.
They are offline fixtures, not uploaded-design or live/native parity evidence.

The first capture run stalled while encoding an image in the widget test fake
async zone. The confirmed owned Flutter runner PID 1927820 was interrupted;
session 57520 completed. Encoding was moved into `tester.runAsync`; no command was
restarted merely because observation expired. A focused diagnostic identified
the exact app Row causing the overflow, then the complete capture matrix passed.

Final validation, all expensive runs sequential and Flutter tests concurrency=1:

- **58** installed Aegis repository/UI/domain review/projection/file/account cases.
- **6** real-theme offline capture cases.
- **1** actual application-shell Ask Aegis launcher/history/Back/draft case.
- Full application analysis: no issues. Cleaned scoped braces, one unused test
  import and explicit mounted guard; also one existing brace in the SDK transport
  test. No suppressed analyzer rules.
- Normal release web build: **65.2 seconds, success**. Optional Wasm dry run also
  succeeds. It reports a missing CupertinoIcons asset warning; inspected app/SDK
  widget source has no direct reference, reviewed Aegis images use MaterialIcons,
  and no native font qualification is claimed. Preserve this warning for runtime
  follow-up rather than treating a successful build as native device proof.
- Normal package URI/lock verification and all changed-repository diff checks pass.

Logs: `/tmp/spartan-mobile-turn28-pub-get.log`,
`/tmp/spartan-mobile-turn28-installed-tests.log` (initial 56),
`/tmp/spartan-mobile-turn28-projection-tests.log` (39 focused),
`/tmp/spartan-mobile-turn28-installed-tests-final.log` (58),
`/tmp/spartan-mobile-turn28-small-layout-diagnostic.log` (original failing Row),
`/tmp/spartan-mobile-turn28-captures-final.log` (6),
`/tmp/spartan-mobile-turn28-analysis-final.log`,
`/tmp/spartan-mobile-turn28-launcher-test.log` (1),
`/tmp/spartan-mobile-turn28-web-build.log`.

### Actual preview rejection and remaining work

Called `handrail_open_mobile_preview_browser` with exact Spartan project
`fd1c7a13-8910-4571-956c-a71ae47e0625`, environment `dev`. Actual tool result:
`preview_browser_access_denied`, boundary `platform_blocker`,
`app_failure_reproduced=false`. Stated reason: the requested project is outside
this Dev Chat's saved project scope; caller-supplied group lists cannot grant
access. No browser/handoff was opened, no Vault secret was fetched and no raw
Flutter listener or alternative scope was used. This blocks that preview route;
it does not establish universal live/tool unavailability.

Updated the mobile qualification document, Spartan web integration/cleanup notes,
Flutter adoption and shared adoption standard with actual installed evidence and
limits. All changes remain uncommitted; no push/PR/publication/deployment or
project/production database/storage write occurred.

Next work remains the full objective:

1. Qualify actual Cents/Spartan JS public baseline adoption from 5d9387 to 5a0ebe,
   keeping HTTPS full SHA/normal matching locks and normal SDK build hooks.
2. Complete Cents visual acceptance without masking/hiding features or increasing
   its bound. Current public image is 0.142436092633 and local Copy candidate is
   0.123638509789, both above the unchanged 0.123000.
3. Complete remaining registered-consumer and retention/cutover audits, saved
   voice/activity presentation and source integration of newer SDK features.
   Spartan's direct domain decision transport still needs the shared durable
   decision controller once that supported replacement preserves financial review.
   Public c22 does not include local deletion/decision/voice changes or the new Copy
   layout. Source tests cannot count as real adoption of those unpublished APIs.
4. Required live/provider/native/audio, missing-font follow-up and coordinated
   production data/storage cleanup remain. This exact preview route requires an
   authorized saved project scope. Release actions and production SQL writes
   remain under their explicit authorization gates.

Keep both goals active. No running test/build process remains at handoff. Overall
completion is unproven and no repeated global impasse has been reached.

## September 14 twenty-ninth autonomous continuation

Resumed existing work after auth/usage recovery without restarting the task.
Verified native and durable goals remain active with the exact objective and its
explicit release restriction. Current owner access retains existing scope. The
Cents install had completed before recovery; no duplicate install was launched
while it was running. No new access rejection occurred in this continuation.

### Actual public web dependency adoption

Cents and Spartan web now pin public assistant 0.2.36 at
`5a0ebe520a9e6fde0b3a792f959a7a10e0a3de50`. Normal npm 12.0.2 install and clean
`ci --include=dev` pass; foreground logs prove SDK prepare/build hooks. Each lock
changes only its root requirements and assistant package node. All other package
nodes, including every other Handrail public SHA, remain unchanged. Spartan
retains financial SDK 0.3.51 and its root postinstall patch; Cents retains financial
0.3.29. Both static adoption gates pass without aliases or alternate SDK sources.

Manifests declare Node 22.23.1/npm 12.0.2, direct Git allowance and explicit
existing build hooks. Docker source uses that same toolchain. Cents' build base
uses glibc Debian to match its existing declared Playwright Noble runtime;
Node/npm are copied from the build into that runtime. Spartan retains both Debian
stages, exact toolchain versions, and financial patch inputs before installation.
The registered mobile consumers' actual public c22 pins are unchanged this turn;
newer local JS/Flutter source fixes still need publication and normal adoption.

### Legacy test and documentation cleanup

Three stale `.catalogFunctions` test calls now use canonical SDK definitions and
current availability with `input_schema`. Exact business-input and authorization
assertions remain. Invoice-accounting discovery now uses `aegisSdkToolNamespaces`
instead of the two removed provider catalog helpers. The HTTP logging fixture
sends raw audio to the shared SDK middleware boundary instead of injecting the
retired multipart transcriber. It checks safe error logging and excludes audio
and credential text. That fixture deliberately stubs the middleware; the mounted
SDK tests separately qualify actual dictation and authorization.

The adjustment-detail fixture now supplies current cancellation capability fields;
the Plaid orchestrator fixture supplies its current expense-match result. No
business runtime behavior or financial pin was changed for these fixture repairs.
Seven test files changed. Existing unrelated `.credit-patch-uZvSzY/` and previous
fresh-baseline work remain intact.

Host setup READMEs now state the actual Node/npm toolchain. Cents no longer claims
to seed a demo chat. Spartan's typecheck documentation describes the real 48-file
batches, replacing its stale single-heap description. Shared/host adoption guides
record current public installs and distinguish local qualification from release.
The historical September 4 rollout report points to this current handoff.

### Validation receipts

All expensive checks ran sequentially; Vitest used one worker.

- Cents: full browser/server/tools typecheck; 152 tests across 15 gateway/domain/
  UI/API-contract files; normal workspace production build; static adoption and
  diff checks pass.
- Spartan: 206 selected tests across 26 files, then 28 fixture cases across seven
  files pass: **220 distinct tests in 29 files**, with overlap counted once.
  This includes the installed UI, mounted gateway, auth, raw dictation, uploads,
  history/reopen, approval review/isolation, exact domain execution, receipt
  recovery, financial integration and its retained patch. Scoped lint passes.
- Spartan source/configuration and **all 496 configured test roots** compile using
  the repository's unchanged compiler settings/global declarations and 48-file
  batches. The initial full run found stale fixtures. To avoid rechecking passing
  batches, temporary `/tmp/spartan-web-turn29-*-typecheck.mjs` copies resumed or
  selected only remaining/affected batches. Final coverage is assembled from
  `typecheck-final2` (source and tests 1–96), `typecheck-remaining` (97–496 except
  the two reported fixture batches), `typecheck-fixtures` (337–384 after the Plaid
  fix), and `typecheck-http` (241–288 after the logging fix). Earlier nonzero logs
  are retained as failure evidence, not described as successful full commands.
- Both full production Docker builds pass, including fresh install, normal
  application compilation, production prune and Spartan's financial patch.
  Images: `handrail-assistant-cleanup-spartan:turn29` (`a83803f8d406`) and
  `handrail-assistant-cleanup-cents:turn29` (`f6ee896eb2cc`). These are local artifacts,
  not published or deployed images.
- Both image smokes pass as non-root with read-only root filesystems and networking
  disabled. They verify Node 22.23.1/npm 12.0.2, installed SDK versions/imports and
  compiled server artifacts. Spartan verifies native Argon2. Cents verifies its
  exact HTTPS SDK lock resolution, SSH2 import, and Playwright 1.62.1 launching the
  declared image's bundled Chromium against a static offline page. No application,
  database, real provider or audio session was started by those image smokes.
- Native PostgreSQL 15 baseline requalification passes with actual public 0.2.36
  imports. Each qualifier owns three disposable databases and invokes real dev and
  compiled production migration entrypoints. Cents: 97 ledger entries, three
  retired tables omitted on fresh setup. Spartan: 142 entries, five omitted;
  current server source was compiled again before this qualifier. Both preserve
  historical tables/ledger and pre-source data; Spartan also preserves executing
  business-receipt evidence. Fixtures/pools/servers are closed by their qualifiers.

Logs: `/tmp/cents-web-turn29-{install,ci,typecheck,tests,build,docker-build,runtime-smoke,native-baseline}.log`,
`/tmp/cents-web-turn29-adoption.json`,
`/tmp/spartan-web-turn29-{install,ci,tests,fixture-tests,lint-final,http-lint,docker-build,runtime-smoke,build-server,native-baseline}.log`,
`/tmp/spartan-web-turn29-typecheck{,-final,-final2,-remaining,-fixtures,-http}.log`,
`/tmp/spartan-web-turn29-adoption.json`, `/tmp/web-turn29-lock-check.json`.
Both browser builds retain their large-chunk warnings. The existing node-postgres
concurrent-client-query deprecation warning remains in Spartan's native qualifier;
its source is not attributed to an unverified module.

### Remaining next work and boundaries

No commits, pushes, PRs, publication, deployments or project database/storage
writes occurred. This is substantive local progress, not a repeated-blocker turn;
keep the goal active. Source/publication/live/data-removal boundaries from earlier
turns remain. Cents mobile's unchanged visual gate still fails. Complete remaining
retention and installed-consumer deletion audits, plus the newer shared feature
source review, before claiming independently authorized source work finished.

A specifically re-observed Flutter source edge remains to fix/qualify:
`HandrailAssistantController._publish` sends every known descriptor ID through an
unawaited `monitor.setConversations(ids)`, while the monitor throws above 10,000
IDs (or invalid IDs). The controller's known set can grow across catalog pages.
Do not hide active/unsettled voice state by silently truncating the list; make
bounded-monitor failure visible and recoverable, with a regression and proper
analysis in the declared Flutter sibling. No Flutter source was edited this turn.

Still distinguish public baseline adoption from adoption of unpublished JS/Flutter
fixes. Live provider/audio/native/mobile validation and approved production
cutover remain necessary; Spartan's prior explicit Mobile Preview scope rejection
is still the precise known blocker for that route. No overall 100% claim.

## September 14 thirtieth autonomous continuation

Verified native and durable goals remain active with the unchanged objective;
previous turn was substantive progress, not a blocker turn. Current verified
access retains existing scope. A project-scoped human-answer Coverage Q&A search
for voice decisions returned zero entries. No new live access request/rejection
occurred. Continued in the declared Flutter sibling; no subagents or release
operations were used.

The controller previously invoked `monitor.setConversations` without observing
its future. More than 10,000 known IDs caused `ArgumentError` before the monitor
reported failure, leaving apparently healthy old state and an unhandled async
error. The known descriptor set can grow across catalog pages. Simply catching
that error or truncating the list would still hide incomplete voice observation.

The monitor now validates incrementally and stops on the 10,001st distinct ID.
Invalid/oversized lists invalidate outstanding read generations, retain every
known call/effect/unread record, mark state unsynchronized and suspend polling
reads. Direct callers still receive the existing ArgumentError. The controller
handles it because the monitor has already published a visible failure. A later
valid scope resumes observation, including the same scope that was valid before
the failure. Neither manual refresh nor an old in-flight reply can turn the old
subset into a synchronized view of the rejected scope.

`HandrailRealtimeWorkspaceState.failure` is a bounded enum. The structural history
binding adds `voiceErrorCode`; the shared widget selects safe strings for scope
limits or invalid references instead of always claiming a transient reconnect.
Unknown codes/raw errors retain the sanitized generic fallback. Active,
unconfirmed, unread and unresolved-effect markers and standard history controls
remain present. No text Send/Stop permission, approval, deletion receipt or
business execution rule changed.

Changes are in client `realtime_workspace.dart` and `assistant_controller.dart`,
shared `conversation_history.dart`, their focused tests and adoption/README docs.
Existing local approval/deletion/media work is preserved. Formatting touched the
already-modified history files; no prior behavior was intentionally removed.
The client README also replaces stale legacy-upload wording with the explicit
standard conversation-binding versus host-managed-file contract.

Validation (sequential, one test worker):
- 12 client monitor/history tests pass, including bounded iteration, direct
  invalid-scope errors, last-known active/unread preservation, polling suppression,
  stale response rejection, recovery to the previous valid scope, a 10,001-row
  catalog loaded through 100-row pages, ordinary reconnect and account disposal.
- 32 existing controller, deletion and permission regressions pass.
- 12 shared history widget cases pass, including narrow text scaling, preserved
  voice markers, safe oversized-history messaging, deletion/account transitions
  and normal retry. Total: **56 source tests**.
- Scoped Dart analysis with `--fatal-infos` and Flutter analysis with `--no-pub
  --fatal-infos` pass. Removed one unnecessary non-null assertion reported by the
  first analysis. Diff whitespace check passes.

Logs: `/tmp/flutter-sdk-turn30-voice-history-tests.log`,
`/tmp/flutter-sdk-turn30-controller-regressions.log`,
`/tmp/flutter-sdk-turn30-voice-history-widget-tests.log`,
`/tmp/flutter-sdk-turn30-client-analysis-final.log`,
`/tmp/flutter-sdk-turn30-widget-analysis.log`. Tests resolve the SDK packages'
own local source. No consumer manifest/pin, release or deployment changed; public
Flutter c22 does not contain this repair. This does not establish provider, audio
playback or device qualification.

Next concrete work remains Cents' unchanged visual-reference failure and the
remaining installed-consumer retention/deletion audit. Newer local JS/Flutter
features still need publication and normal public-SHA adoption after authorization.
Retain the explicit release restriction and separate production database approval.
Keep the full goal active; no repeated global blocker streak exists.

## September 14 thirty-first autonomous continuation — Cents source visual gate

Resumed the existing workspace after authentication/usage rotation; verified
both goals remain active with the exact objective and release restriction.
Project-scoped human Coverage Q&A for Cents returned no entries. Previous changes
were preserved. No global blocker streak exists because this turn made source
progress; source completion remains separate from actual adoption and rollout.

Inspected the pinned external reference, installed-public render and turn 27
source render. The last two use the same Cents test conversation, while the
external authority contains different example content. Its original normalized
pixel bound remains useful as a regression gate, not a complete design parity
claim. Did not alter the authority, masks, threshold, conversation fixture or
feature availability to obtain a pass.

Found Cents' global `InputDecorationThemeData.constraints` imposes a 48-pixel
minimum field height. The SDK's collapsed editor inherited it despite supplying
its own minimum and toolbar. `HandrailComposer` now explicitly supplies unconstrained
form dimensions and no disabled input border, keeping sizing and the surrounding
border SDK-owned. Host fonts/colors remain; multiline text grows and all standard
controls keep their touch targets. This frees 22 vertical pixels for Cents'
transcript. No host runtime code or dependency pin changed this turn.

The separate source render now measures **0.12282185395988604 <= 0.123000**.
The installed public c22 image remains **0.142436092633**; it does not acquire
local source changes. The source review reused the explicit temporary package
graph from turn 27, without editing the public cache or normal app graph.
After source captures/reference checks, all six normal app goldens were restored
byte-for-byte and the normal graph hash was verified unchanged.

Validation, sequential and one worker:
- 24 SDK composer/transcript cases pass, including a new hostile host form theme
  with fixed 500x120 constraints in a 320-wide viewport. The input starts compact,
  grows with lines, stays above the complete toolbar and sends the exact text.
- 11 SDK workspace cases pass, including narrow/double text, Stop, retained drafts
  and account/approval isolation. Total **35 SDK cases**.
- **39** Cents SDK/sheet/alias/contract cases pass against source SDK packages.
- **6** source populated/error/New capture cases in light/dark pass, including
  complete negotiated toolbar checks. **51** Cents authority and route matrix
  cases pass; this includes both previously failing visual gates.
- Scoped Flutter analysis (`--no-pub --fatal-infos`) and diff checks pass.

Evidence: `/tmp/cents-mobile-turn31-source-goldens/` has images, generation and
reference logs, exact measurement and restoration receipts.
`/tmp/cents-mobile-turn31-source-tests.log`,
`/tmp/flutter-sdk-turn31-composer-transcript-tests.log`,
`/tmp/flutter-sdk-turn31-workspace-tests.log`,
`/tmp/flutter-sdk-turn31-analysis.log`. The measurement uses the repository's
existing Dart PNG decoder; no image dependency was installed. Updated Cents,
Flutter and shared adoption docs to distinguish the new source pass from the
unchanged installed-public gate.

Next concrete work: complete the remaining retention/deletion and registered
consumer audit against current source, including outstanding production cutover
inventories. Preserve the source/public/live distinctions. Reviewed SDK public
commits and actual consumer adoption remain unauthorized by this objective;
production SQL/storage cleanup requires its separate approval path. No commit,
push, PR, publication, deployment, production data write or live audio/provider
qualification happened in this turn.

## September 14 thirty-second autonomous continuation — provider retention audit

Verified native and durable goals active with the exact objective and explicit
release restriction. The previous turn made source progress, not a blocked wait;
this turn also makes source progress, so there is no global blocker streak.
Coverage Q&A for human retention decisions returned no entries in current scope.
Read the actual Mills/Cents/Aegis runtime wiring and shared deletion contract.

Cents uses the standard SDK OpenAI provider with its existing conversation-bound
retention. Mills' protected attachment PUT already binds its receipts to the
household conversation. Aegis' custom provider used unbound operations and
continuations despite the installed SDK supporting bindings. Added public
`forConversation` calls to `provider-invocations.ts` and `sdk-provider.ts`, including
the expense-receipt continuation read. The asynchronous execution context supplies
the trusted conversation. Owner/tenant scopes, hashed operation IDs and request
fingerprints are unchanged; no new key is used to evade an old retained claim.

A real mounted SDK deletion fixture now runs two Aegis conversations with a
business action in each. Deleting one removes its continuation and scrubs its two
completed provider results to minimal receipts with unchanged fingerprints. The
other conversation's provider/continuation records, actual tool ledger and usage
receipts remain unchanged. The target cannot reopen and exact deletion replay is
idempotent without further provider or business dispatch.

The lost-response restart case proves an unfinished conversation-bound provider
claim blocks deletion after the resumed text turn fails. Separate snapshots with
an old unbound or mismatched provider record prove authenticated server recovery
fails closed, retains the original record and pending review, and performs no
provider/business dispatch. No record repair/import is added. Old records still
need approved cutover inventory and outcome settlement before disposing content.

Validation, actual public JS 0.2.36 installation, sequential/one worker:
- 26 provider cases pass in `/tmp/spartan-web-turn32-provider-tests-final.log`.
- Two additional binding/recovery cases pass in
  `/tmp/spartan-web-turn32-provider-binding-tests-final3.log` (26 skipped).
- 29 native composition/mounted gateway/domain-approval cases pass in
  `/tmp/spartan-web-turn32-gateway-tests.log`. Total **57 distinct cases**.
- Scoped source/test TypeScript compilation with original options and global
  declarations passes: `/tmp/spartan-web-turn32-typecheck.log`. Its temporary
  wrapper is `/tmp/spartan-web-turn32-typecheck.mjs`; no repository config changed.
- Scoped ESLint and diff checks pass: `/tmp/spartan-web-turn32-lint.log`.

Initial test expectations were corrected after inspection: only tool-bearing
provider responses create continuation rows, and an adapter-returned cancelled
terminal result is a completed operation receipt, unlike an unfinished claim
captured at process loss. The unbound-recovery fixture initially raced automatic
server recovery with a browser retry, producing a revision conflict. The final
fixture observes the owned durable server turn and proves no dispatch/record
mutation. Original failure logs remain; no application fallback or test threshold
was weakened to conceal a source failure.

Updated Aegis integration/cutover docs and SDK deletion guidance. Corrected stale
Mills documentation that still described the completed source workspace conversion
as pending, while retaining the distinction from actual installed Flutter adoption.
No dependency pins/locks, SDK implementation, application images, commits, pushes,
PRs, publication, deployments or production data changed this turn. The fix uses
existing public APIs and applies after an authorized Aegis host release. The turn
29 container images predate this source correction; they were not silently updated.
PGlite/synthetic provider fixtures do not prove live provider or audio behavior.

### Next concrete audit work

1. **Mills transcription** still uses `/api/v1/assistant/transcriptions` in web
   `src/client/assistant/assistant-api.ts`, the installed mobile adapter and source
   candidate `lib/repositories/sdk_assistant_repository.dart`. The server route
   in `src/server/assistant/routes.ts` saves `{text}` under unbound
   `mills-transcriptions` tenant/environment records configured by `index.ts`.
   Household chat deletion cannot identify these copies. Replace with supported
   SDK transcription wiring while preserving role/CSRF, signature checks, current
   session authorization, retry identity and usage. Both consumers must move
   before removing an active route. Update the old-record cutover inventory;
   do not infer ownership from transcript text or run an automatic purge.
2. **Shared conversation file adapter:** `src/server/conversation-files.ts` still
   retains consumed staging metadata in a synthetic account upload scope; cleanup
   is only triggered by another stage call. Complete idle expiry and deletion
   linkage qualification, preserving shared/business objects and durable request
   identities. The old generic `import` helper also remains in this adapter;
   audit actual registered use before removing an API. Aegis no longer calls it.
3. Continue the remaining registered-consumer/source audit, then actual public
   adoption/authorized rollout/live qualification and approved production cutover.
   Keep overall completion unproven until all required evidence exists.


## September 14 turn 33 — Mills conversation-bound dictation

Resumed the existing thread after auth/usage rotation. Verified the durable and
native goals active with the same objective and source-only release restriction.
Current context retained existing action-specific authority; no access rejection
was inferred from the owner capability diagnostic. The prior turn's dictation
Coverage Q&A lookup returned no human entries. Existing unrelated changes were
preserved. No subagents, new goals, release actions or production writes.

The active `/api/v1/assistant/transcriptions` route was storing `{text}` in an
unbound `mills-transcriptions` tenant/environment provider operation. That result
could not be found safely by household conversation deletion. Removed the custom
route, its independent raw parser/provider operation/usage composition, multipart
HTTP/provider loop and web binary helper. The high-level Mills assistant now
configures the existing public `openaiTranscription` adapter. Mills retains provider
settings, vocabulary, timeout policy, 4,000-character output limit, live session,
admin/member role, CSRF and per-user rate checks. A bounded WAV prefix check keeps
the existing 44-byte minimum before operation claim; generic format/signature,
size/duration, timeout, replay, admissions and audio usage remain SDK-owned.
Unconfigured dictation is negotiated as unavailable while history remains usable.

Web uses the SDK HTTP transcriber/capability with its existing CSRF/expiry seam.
Both Flutter source trees use the installed public SDK transcriber, preserving
conversation identity, measured duration and original capture key; their custom
binary decoder, WAV signature helper and key hashing are removed. Session lifetime
checks and live voice/financial coordination remain host-owned. The installed
screen now creates the first mutable draft before recording, keeping a stable
conversation through retry. The candidate retains its shared workspace binding;
it still relies on newer unpublished SDK changes for unrelated features.

All this dictation wiring uses actual public JS
`5a0ebe520a9e6fde0b3a792f959a7a10e0a3de50` and Flutter
`c22b5ac97b0bcabd99b2d96995a0ed85c49ec124`. No manifest/lock or SDK implementation
change was made for turn 33. Candidate Flutter calls used `--no-pub` and its known
local source graph; those checks do not prove installed source-candidate adoption.

Qualification (expensive checks sequential, configured workers retained):

- **64 distinct web/server tests pass** across assistant persistence/routes,
  transcription provider, web voice controls, client negotiation, runtime and
  retained auxiliary outbox. The 54-case broad run initially had two stale
  expectations; focused corrections verify the SDK's 400 invalid duration, 501
  unconfigured feature and shared attempt ownership of failure usage. All other
  broad-run cases passed; none were weakened or silently skipped for completion.
  `/tmp/mills-web-turn33-tests-final.log` (52 passing plus the two historical
  failures), `/tmp/mills-web-turn33-corrections-tests.log` (provider/deletion/outbox
  passed; the remaining 501 expectation corrected next), and
  `/tmp/mills-web-turn33-validation-runtime-tests.log` (10 passed, including final
  validation plus all nine runtime cases). The previous three owned dictation
  route cases also passed in `/tmp/mills-web-turn33-gateway-tests.log`.
- Owned PGlite + mounted Express/SDK fixtures verify concurrent/restart replay,
  no duplicate physical provider dispatch, changed-audio conflict, role/CSRF and
  cross-user isolation, audio intake/rate limits, old-route absence, timeout and
  late provider success, unfinished-claim deletion refusal, and deletion of new
  dictated text while another conversation and usage receipts survive. Synthetic
  provider usage is recorded; no live provider or actual audio is claimed.
- **Scoped TypeScript passes** `/tmp/mills-web-turn33-typecheck-final2.log`, using
  original compiler settings and repository ambient declarations; source imports
  include server composition and changed web UI. Initial missing ambient fixture
  scope and incomplete typed audio evidence were corrected. Scoped ESLint passes
  `/tmp/mills-web-turn33-lint-final.log`.
- **Installed mobile: 126 tests pass**: 22 host tests
  `/tmp/mills-mobile-turn33-host-tests.log` + full 104 screen cases
  `/tmp/mills-mobile-turn33-full-screen-tests.log`. The earlier five dictation
  cases caught first-chat missing conversation identity; the source fix prepares
  that draft before recording. The focused rerun and then complete screen suite
  pass. Scoped analysis passes `/tmp/mills-mobile-turn33-analysis-final.log`.
- **Shared-workspace source candidate: 41 host/screen tests pass**
  `/tmp/mills-candidate-turn33-tests.log`; scoped analysis passes
  `/tmp/mills-candidate-turn33-analysis-final.log`. Removed now-unused decoder/
  identifier helpers surfaced by analysis. These checks retain account-switch
  cancellation, original-key retry, pending insertion and live/capture exclusivity.
- Updated primary/mobile cleanup and qualification handoffs plus SDK deletion
  guidance. Source diff checks pass. No app image rebuild, provider/audio/native
  qualification, commit, push, PR, SDK publication, deployment or database/object
  deletion occurred. The current host source applies only after coordinated release.

### Remaining work after turn 33

1. **Shared conversation file adapter idle staging retention/import API:** the
   next item from turn 32 remains. Audit all registered consumers before deleting
   an API, implement idle cleanup/linkage with shared/business storage protection,
   qualify active writers and stable request identities. Use owned fixtures only.
2. **Mills auxiliary usage cleanup:** `beginMillsAIRuntimeUsage` and its old receipt
   projection in `src/server/assistant/ai-runtime-usage.ts` no longer have a
   production caller after dictation moves. The same module's client/worker is
   still composed for live usage and retained outbox delivery. Remove genuinely
   unused projection/types/fixture dependence; audit the active delivery wiring
   against supported SDK adapters before replacing it. Preserve original live
   receipt/outbox identities and financial behavior. This is independently
   authorized source work, not a production cleanup or a reason to mark blocked.
3. Require compatible web/server and mobile versions for the retired dictation
   route cutover. Old unbound `mills-transcriptions` documents need explicit
   approved inventory/removal; never infer ownership from text or auto-purge.
4. Finish remaining registered-consumer/source audits, then record exact newer
   SDK publication/adoption, release, live/native platform and production approval
   dependencies. The goal stays active; no repeated global blocker streak exists.

## September 14 thirty-fourth autonomous continuation

Resumed the unfinished retained-file work after auth rotation without resetting
any checkout. Verified current context/access plus active native/durable goal;
the explicit objective still prohibits release actions. Relevant current-project
Coverage Q&A search for human attachment-retention answers returned no entries.

Removed the unused `createConversationFileStorage.import` operation after checking
all three scoped hosts; Aegis uses stage/materialize/download only. The adapter's
new blob allocation and staging admission share a transaction. Retained bytes,
upload consumption and the actual conversation binding also commit together.
Consumption failure leaves the original upload usable, with no orphaned saved
copy. Materialize/download recheck current authorization before returning content.
Deletion now follows both ordinary and retained conversation bindings, removing
linked staging metadata and only SDK blobs without another attachment reference.

Added a versioned policy to newly staged records and an explicit bounded SDK
expiry worker. It rechecks trusted service/tenant scope, strict target shape,
versions and conversation/blob locks. Idle and restarted apps can expire temporary
records without another upload. It does not sweep immediately at startup, mark old
records, remove malformed targets or delete saved/shared/business files. Existing
unmarked uploads/imported copies remain an approved cutover task. The worker must
be started once per authorized service and stopped before the SQL pool closes.

Native PostgreSQL qualification caught a driver defect that PGlite did not:
postgres.js tried to serialize retained expiry `infinity` as a JavaScript Date.
The blob store now binds text and casts to timestamptz in PostgreSQL. The fixture
uses independent connections in an owned disposable cluster and accepts no
existing database settings. Five cases pass: competing conversation retention,
expiry discovery racing consumption, expiry winning the lock before retention,
deletion fencing late retention, and deletion waiting for retained consumption
before removing both saved and staging records. Two competing expiry workers
collect the staging record once; saved bytes remain downloadable after expiry.
Every native fixture run stopped and removed its own cluster.

Final verification: 51 tests across conversation files, staging, deletion,
external cleanup and PostgreSQL persistence, with one Vitest worker; full SDK
TypeScript, scoped ESLint and normal build pass. Logs:
`/tmp/sdk-js-turn34-final-tests.log`,
`/tmp/sdk-js-turn34-typecheck-final.log`,
`/tmp/sdk-js-turn34-build.log`,
`/tmp/sdk-js-turn34-lint-final.log`,
`/tmp/sdk-js-turn34-native-files.log`.
Corrected stale shared retained-file/history adoption documentation and added an
exact Aegis service adoption handoff. No consumer manifest, lock, runtime, database
or storage resource was changed. Public `5a0ebe5` lacks these corrections; a source
qualification or temporary alias is not installed-consumer completion. No SDK
publication/commit/push/PR/deployment or production cleanup occurred.

Turn 34 additionally finished the next independently authorized Mills source step.
The removed dictation route had left `beginMillsAIRuntimeUsage`, its custom
admission/settlement and receipt builder, and audio-evidence forwarding with no
production caller. Removed those APIs and the host retry timer. The 27-line host
composition now uses the **installed public 0.2.36** environment client, receipt
sink and `createAIRuntimeUsageDelivery`; no newer SDK release is needed for this
part. The auxiliary outbox tenant and service scope are unchanged, including old
receipts. Active live voice continues its trusted owner/session attribution,
duration evidence and backend receipts. The shared assistant keeps its configured
backend model; only obsolete auxiliary transcription configuration was removed.

Mills verification: eight installed-SDK tests for configuration, intervals,
shutdown while a flush is pending, SQL durability through outage/restart and lost
acknowledgement, historical receipt deduplication, subsequent current live backend
receipt delivery, environment isolation and voice evidence safety. Scoped
TypeScript includes the full server composition/import graph and all three usage
test files; ESLint passes. Logs: `/tmp/mills-web-turn34-usage-tests-final.log`,
`/tmp/mills-web-turn34-typecheck-final.log`, `/tmp/mills-web-turn34-lint-final.log`.
Corrected the host's historical SDK compatibility handoff to distinguish public
0.2.34 limitations from the currently installed 0.2.36 APIs. The separate SDK
package-contract run also passed all 31 cases
(`/tmp/sdk-js-turn34-package-contract.log`). These checks do not exercise real
telemetry billing, deployed providers or native audio playback. No release or
production writes occurred.

Late turn 34 shared-workspace update: external commits appeared at 07:41 Central.
Read-only public `git ls-remote` confirmed JS `15a3806c2595a3f93a87a768ad13293113f41b58`
(0.2.37) and Flutter `50fe566d73f68b2beacc2a874dc9a038363b1509`. Both contain the
previously qualified local source. This goal run did not commit or push them.
Mills web also moved externally to `05699556c55615585635d8792978786f7f58cd95` and
Spartan web to `9da231f09158569dca6916168f918087cacbffec`; existing work was preserved.
Mills manifest, lock and installed package remain JS 0.2.36/`5a0ebe5`; Spartan's
manifest also remains that SHA and has no file-expiry worker yet. Reverify other
consumer states before adoption. These public releases resolve the earlier SDK
publication dependency, so it is no longer a valid blocker. Dependency adoption
and local qualification remain authorized; this run's commit/push/PR/deployment
restriction and native production-write approval requirements remain in force.
The next continuation must use these exact verified public baselines, not stale
claims that the source is unpublished. No overall completion claim is warranted.

## September 14 thirty-fifth autonomous continuation

Verified active native/durable objectives and current context/access. Relevant
human Coverage Q&A search for SDK adoption returned no entries. Verified the public
JS `15a3806` and Flutter `50fe566` repositories before changing consumers. No commit,
push, PR, publication or deployment is authorized for this goal run.

All three web manifests/allowScripts entries now pin the exact full public JS SHA.
Normal npm 12 installs compile the SDK; only root metadata and the assistant
package changed in each lock. The financial SDK pins and Spartan's normal
postinstall patch are preserved. Static adoption checks pass in all three hosts.
Mills typecheck/build plus 187 distinct selected tests pass. The first direct
Vitest invocation inherited production React and failed 51 browser assertions;
using the repository's test launcher passed all 69 browser cases. The other
118 node cases had passed. Cents typecheck/build and 152 selected cases pass.

Aegis starts one SDK staging-expiry worker after its assistant initializes and
awaits it before usage flush/pool close. It uses the existing default
`conversation-files` partition, fixed safe diagnostic codes and current file
identities. Seven installed attachment cases include idle managed expiry, saved/
unmarked preservation, shutdown and linked deletion. Twenty-eight provider cases
and 56 UI/live-delivery cases pass. Full source/config plus all 496 test roots
compile with the configured 48-file sequential batches. Two fixture annotations
were corrected without changing behavior. Mounted API/native composition have
22 passes and one failure: a later dismissal after execution returns 400.

That mounted failure exposed the public SDK's missing response branch when the
coordinator returns `already_decided` without making a new transition receipt.
The new source-only fix returns the authorized terminal proposal in that branch;
exact retries still return the original immutable decision receipt. It reproduces
as two failing SDK cases before the fix (short and paginated history), then all
22 assistant/coordinator cases, typecheck, lint and normal build pass. The host
assertion remains unchanged and **still fails on installed public 0.2.37**. This
is a corrected-SDK publication/adoption dependency, not installed qualification.
No installed package was edited or aliased to hide it.

Main Mills mobile received the reviewed source candidate's shared account/
workspace/file/voice/financial-review wiring, removing its duplicate chat runtime
and old playback files. Compared main against candidate base `b9049406d954ebc4115ce13c6a7f9f882d9476c4`;
the conflicting source changes were the known intervening SDK dictation cleanup,
which the shared candidate also retains. Qualification history was merged.
Pre-change source copies and the merge plan are in `/tmp/mills-mobile-turn35-merge/`.
All three main mobile manifests/locks and generated roots now resolve public
Flutter `50fe566` normally. Mills changed only the two assistant dependencies;
Spartan/Cents additionally resolve the SDK's WebRTC transitive dependencies.
Mills main full analysis, 312 selected cases and release web build pass (70.9s).
Aegis passes analysis, 179 selected cases and release web build (64.0s). Its first
combined run passed 178 and one stale financial notice assertion failed; the
assertion now matches the existing screen and dedicated tests, and its focused
rerun passes. No financial behavior changed. Cents passes full analysis, 96
selected cases and release web build (61.5s). Its installed regenerated goldens
now pass the unchanged visual bound at 0.12282185395988604; authority files and
thresholds are unchanged. Populated light/dark captures were inspected, including
the remaining test-font missing Greek glyph. The six captures exactly match
prior turn-31 source captures, and actual normal installed tests provide adoption
evidence. Mills then removed unused direct WebRTC declarations; only dependency
classification changed, and full analysis plus ten voice-adapter cases pass. The detached candidate now uses public roots, not source
aliases. No other active worktree was touched.

The Flutter gateway test consumer's old JS `d574ea7` pin is now aligned to
public `15a3806`. npm 12 initially rejected Git fetching with `EALLOWGIT`. Its
project lacked the declared root-Git setting used by the qualified hosts. Added
`.npmrc` with `allow-git=root`, the exact pinned SDK lifecycle allowance, and the
Node/npm development-tool contract; no global npm setting was changed. Normal
installation succeeds. The formerly candidate-only attachment gateway now uses
the installed dependency by default and is not skipped. Full client analysis and
all 138 cases pass, including actual HTTP deletion/approval recovery and file
ownership/expiry. Source overrides remain explicitly separate.

Next independent retention audit: `rg cleanupExpired src` in the SDK finds only
the generic staging method's declaration. Ordinary high-level uploads (Cents
uses `attachmentUpload:true`) do not use the new retained-file marker/worker.
SQL expiry prevents reads but does not prove idle physical byte/metadata cleanup.
Implement new managed generic staging expiry after adoption qualification, keeping
old unmarked records for approved cutover and preserving shared references and
active/uncertain work. Detail: `/tmp/assistant-goal-turn35-next-audit.md`.

Current logs: `/tmp/{mills,spartan,cents}-web-turn35-*`,
`/tmp/{mills,spartan,cents}-mobile-turn35-*`,
`/tmp/mills-candidate-turn35-pub-get.log`,
`/tmp/flutter-gateway-turn35-*`, `/tmp/sdk-js-turn35-*`.
A fresh scaffold frozen to public 15a passes normal install, check, clean ci,
repeated check, static adoption and the native disposable PostgreSQL fixture.
The first check failed devEngines because a directly invoked npm 12 CLI inherited
npm 10 on PATH for nested scripts. Selecting npm 12 on PATH resolved it without
changing the scaffold. The runtime fixture confirms one synthetic provider call,
one usage receipt, fresh/repeated migration, auth/isolation, restart history and
archive/restore; all owned processes/database resources are closed and removed.
Logs: `/tmp/sdk-js-turn35-scaffold-*`.

Mills and Cents dev Mobile Preview calls explicitly targeted their project IDs
and were rejected outside saved scope with `preview_browser_access_denied`,
`boundary=platform_blocker`, `app_failure_reproduced=false`. No browser opened.
The existing Spartan rejection remains separate evidence. No raw listener,
credential route or caller-supplied group bypass was attempted.

Ordinary upload admission is also fixed in newer unpublished SDK source. Generic
staging snapshots caller bytes/scalars before its first await; the PostgreSQL
bundle snapshots before pool acquisition and commits bytes/metadata together.
Failed metadata admission leaves no orphan; a lost commit acknowledgement returns
unavailable without compensating deletion, and exact replay recovers the original
identity. Stage success diagnostics occur after the acknowledged commit. Three
regressions failed before the fix, then 52 focused staging/deletion/foundation
cases passed, including a fourth lost-commit case. Full typecheck, scoped lint and
normal build pass. The expanded native PostgreSQL checker passes all eight cases:
rollback and concurrent exact retry, deletion-before-admission, lost commit reply,
and the five existing retained-file consumption/expiry/deletion races. It creates
and removes its own disposable cluster and accepts no project database URL.
Logs: `/tmp/sdk-js-turn35-staging-{before,tests-final,typecheck,lint,build,native}.log`.

All confirmed command handles are closed. Workspace whitespace checks pass in
all nine involved checkouts; `/tmp/assistant-goal-turn35-workspace-receipt.json`
records heads and changed counts. No source commit/publication/deployment or
production data write occurred. Normal consumer adoption qualification is
complete to the limits above; safe ordinary managed idle expiry and the remaining
retention/legacy audit continue as independent work. The source approval/admission
fixes are newer than public 15a and need later actual adoption. No overall
completion or global blocked status is claimed.

## September 14 thirty-sixth autonomous continuation — managed upload retention

Resumed the unfinished work after account rotation, verified the unchanged active
native/durable goal and preserved all existing work. The bounded-goal access
contract remained `existing_action_specific_authorization`. Human Coverage Q&A
for retention returned no answers. The objective's explicit release restriction
and production-write approval boundary remain in force.

Ordinary PostgreSQL uploads now receive a new version 2 retention marker in a
trusted service partition. `createHandrailAssistant` owns one bounded idle worker,
with no immediate startup sweep, no per-request timers and no adoption of old
unmarked rows. Explicit service scheduling and principal-scoped cleanup use the
same safe scanner. Deletion's readiness check is factored without mutations:
active/uncertain text, voice, provider, tool and approval work blocks expiry;
history, checkpoints, voice read state and required receipts remain intact.
Expiry rereads metadata/version under conversation/blob locks, verifies the blob
lease and preserves shared/business references. Worker flushes coalesce and
shutdown joins in-flight cleanup before SQL closes.

Shared-file consumption and retry identity needed additional corrections.
PostgreSQL consumption now commits its metadata change and guarded byte removal
together. Both ordinary and retained-file expiry retain the same minimal hashed
`attachment.expired` receipt. Retained-file stage admission shares the upload
identity lock with expiry and captures the upload key before waiting for storage.
An old retry cannot recreate bytes after cleanup/restart; a conflicting request
conflicts, and saved files remain available through their existing references.

The resumed audit reproduced malformed-history starvation, malformed-key cursor
failure, retained-file first-batch starvation, and retained-file retry allocating
a new copy after expiry. Each regression is now fixed. Corrupt history blocks
only its own conversation; temporary database failures remain unavailable.
Both workers advance past blocked rows, including malformed SQL keys, without
using scan positions as deletion authority. The retired staging `blob_` alias,
fallback lookup and retained-file materialization grammar are removed. Old data
is preserved for the approved cutover; business blob-key contracts are unchanged.

The stable worker-stop alias now joins the attachment lifecycle; the new
`stopBackgroundWorkers()` name is also accepted by the adoption checker. The
scaffold and all three web hosts await shutdown before closing SQL. Mills uses
`finally` so a failed final usage flush still stops cleanup. These host changes
compile against their unchanged normal public 15a installations.

Final SDK validation: 80 cases across seven files pass with one Vitest worker;
full TypeScript, scoped lint and normal build pass. Twelve native PostgreSQL
cases pass, including a retained upload retry waiting behind expiry, concurrent
workers/materialization, changed leases, deletion races and lost commit replies.
The fixture stops/removes its own cluster and accepts no project database URL.
Package/adoption CLI contracts pass 39 cases. Final logs:
`/tmp/sdk-js-turn36-retention-final-{tests,lint,build,native,contract}.log` and
`/tmp/sdk-js-turn36-retained-receipt-typecheck.log`. Earlier failure receipts are
`/tmp/sdk-js-turn36-{expiry-unreadable,cursor-before,retained-receipt-before}.log`.
The final suite supersedes the earlier overlapping 73/77/79-case runs.

A fresh public-15a scaffold passes normal npm 12 install, client/server typed
checks/build, clean `npm ci`, repeated checks/build and static adoption with
matching public HTTPS locks. Its owned PostgreSQL runtime fixture passes fresh
and repeated migration, fail-closed auth, ownership isolation, create/send,
permission revocation, restart history, archive/restore and usage outage/recovery
with one synthetic provider call and one usage receipt. See
[the scaffold qualification](scaffold-qualification.md). This verifies the updated
template against public 15a, not installation of the newer retention source.

Mills full TypeScript and Spartan/Cents server TypeScript checks pass after their
shutdown edits: `/tmp/{mills,spartan,cents}-web-turn36-shutdown-typecheck.log`.
All nine workspace diff checks pass; heads and changed counts are recorded in
`/tmp/assistant-goal-turn36-workspace-receipt.json`. All command handles and owned
database fixtures are closed. Flutter/mobile source and pins remain as qualified
in turn 35. No commit, push, PR, SDK publication, deployment or production write
occurred. Current JS HEAD remains public 15a; these source corrections require a
new public committed revision before actual consumer adoption.

The closing acceptance audit also removed the unused
`resolveLegacyTurnMessageId` migration callback from canonical timeline, React
transcript and styled props. No scoped consumer used it. Thirty-one timeline,
approval, follow/presentation and launcher cases pass. Four initially failing
presentation assertions were stale: domain cards occupy a separate timeline slot,
and hiding the picker preserves catalog hydration/restoration. Corrected those
assertions and documentation without changing those established runtime behaviors.
Full TypeScript, scoped lint and normal build pass; package/adoption contracts
pass all 39 cases again. Logs:
`/tmp/sdk-js-turn36-timeline-removal-{final-tests,final-typecheck,lint,build}.log`
and `/tmp/sdk-js-turn36-close-contract.log`. Retention/native checks were not
repeated after this isolated presentation change. The adoption guide's stale
c22/source-only mobile paragraphs now reflect installed public 50fe qualification.

[The acceptance inventory](assistant-cleanup-acceptance.md) closes the source and
handoff review. No additional independently actionable omission was identified
in that review. Remaining gates are a permitted corrected public JS release and
actual consumer adoption; authorized runtime/native qualification; and approved
production database/storage cleanup. The selected objective explicitly forbids
SDK publication/commits/deployment. Latest Mobile Preview evidence remains the
exact Mills/Cents turn 35 and Spartan turn 28 `preview_browser_access_denied`
results; these do not establish that all live routes are unavailable. Local
fixtures do not establish provider execution or audio playback.

This is the first end-of-turn audit at those remaining dependencies after closing
the independently actionable source work. Keep the goal active unless the same
blocking condition persists for the native goal's required consecutive turns;
do not claim overall 100% or invent permission from elapsed time. Earlier parts
of this turn made substantive source progress and are not repeated blocked turns.
The final nine-repository snapshot is
`/tmp/assistant-goal-turn36-workspace-close.json`; all heads are unchanged and
all diff checks pass. The native goal remains active at this first dependency
audit, with no running command or owned database/browser fixture left behind.

## September 14 thirty-seventh autonomous continuation — dependency audit 2

The prior turn was substantive source progress. Reverified the active native and
Handrail goals: the objective still explicitly forbids commits, SDK publication
and deployment. Both the local JS HEAD and the public repository's default HEAD
remain `15a3806c2595a3f93a87a768ad13293113f41b58`; no newer public correction is
available to adopt. The reviewed local changes and acceptance inventory remain
present, with a clean whitespace check. There is no live build/test/job handle
to wait on and no reason to repeat completed expensive checks.

Retried the supported Mills development Mobile Preview route with its explicit
project ID. Handrail again returned `preview_browser_access_denied`,
`boundary=platform_blocker`, `app_failure_reproduced=false`: the project is outside
this Dev Chat's saved scope. No browser opened. The available production-browser
handoff is restricted to Handrail's own declared production site; arbitrary host
targets are unsupported. These facts do not prove every possible live route is
unavailable, but provide no supported Mills qualification route in this run.

No independent implementation step remains identified after the acceptance review.
This is the second consecutive end-of-turn audit of the same release/adoption,
runtime-access and production-approval dependencies. Keep the full goal active
for now; mark it blocked only if the next goal turn revalidates the same impasse.
This dependency check is not a verified wait or new source completion.

## September 14 thirty-eighth autonomous continuation — dependency audit 3

Reverified the current context and unchanged selected objective. The local JS
HEAD and public default HEAD remain
`15a3806c2595a3f93a87a768ad13293113f41b58`; the remaining corrections are still
unpublished. The workspace whitespace check passes. The acceptance inventory
and existing validation receipts remain applicable; no source, runtime, access
or approval change removes the previously verified dependencies.

This is the third consecutive dependency audit after the independently actionable
source review was finished. The Handrail goal update returned `ok: true` with
status `blocked`, and the native goal update also returned `blocked`. Neither
goal is complete. Resumption needs an applicable change to release authorization,
permitted live/native qualification access or approved production cleanup scope.
The latest Mills preview receipt remains the turn 37
`preview_browser_access_denied` platform blocker; no browser opened or app failure
was reproduced. No commit, push, PR, SDK publication, deployment or production
write occurred. There are no outstanding build/test/browser/database handles.
